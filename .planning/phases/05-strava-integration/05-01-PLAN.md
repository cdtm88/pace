---
phase: 05-strava-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/db/schema.ts
  - drizzle/0003_strava_token_columns.sql
  - drizzle/0004_session_strava_activity.sql
  - src/lib/strava/crypto.ts
  - src/lib/strava/crypto.test.ts
  - src/lib/strava/match.ts
  - src/lib/strava/match.test.ts
  - src/lib/strava/client.test.ts
  - src/lib/strava/tss-chart.test.ts
  - src/lib/db/queries.ts
  - src/lib/session.ts
  - src/env.ts
  - public/connect-with-strava.svg
autonomous: false
requirements: [STRAVA-05, STRAVA-01, STRAVA-03, STRAVA-04, PROG-02]
user_setup:
  - service: strava
    why: "OAuth credentials + token encryption key must exist in env before any Strava code runs"
    env_vars:
      - name: STRAVA_CLIENT_ID
        source: "Strava → Settings → API Application → Client ID"
      - name: STRAVA_CLIENT_SECRET
        source: "Strava → Settings → API Application → Client Secret"
      - name: TOKEN_ENC_KEY
        source: "Generate locally: openssl rand -base64 32 (must decode to exactly 32 bytes)"
      - name: APP_BASE_URL
        source: "Full deployment origin, e.g. https://pace.app — used to build the OAuth callback URL"
    dashboard_config:
      - task: "Set Authorization Callback Domain to the APP_BASE_URL host (e.g. pace.app)"
        location: "Strava → Settings → API Application → Authorization Callback Domain"

must_haves:
  truths:
    - "encryptToken(x) followed by decryptToken returns x exactly"
    - "Encrypted output is not equal to the plaintext input and differs across two encrypt calls of the same input (random IV)"
    - "matchActivities returns a match only when calendar date matches AND elapsed_time is within +/-20% of totalDurationSec"
    - "Strava schema migrations exist for token columns and session strava_activity_id"
    - "TOKEN_ENC_KEY decodes to exactly 32 bytes or the app throws at import"
  artifacts:
    - path: "src/lib/strava/crypto.ts"
      provides: "AES-GCM encryptToken / decryptToken via Web Crypto API"
      exports: ["encryptToken", "decryptToken"]
    - path: "src/lib/strava/match.ts"
      provides: "matchActivities(activities, sessions) date+duration matcher"
      exports: ["matchActivities"]
    - path: "drizzle/0003_strava_token_columns.sql"
      provides: "ADD COLUMN token fields to strava_connections"
      contains: "strava_athlete_id"
    - path: "drizzle/0004_session_strava_activity.sql"
      provides: "ADD COLUMN strava_activity_id to training_sessions"
      contains: "strava_activity_id"
    - path: "src/lib/db/queries.ts"
      provides: "findStravaConnectionByUserId, upsertStravaConnection, deleteStravaConnection, updateStravaTokens, updateSessionStravaMatch, listSessionsForUser"
      exports: ["findStravaConnectionByUserId", "upsertStravaConnection", "deleteStravaConnection", "updateStravaTokens", "updateSessionStravaMatch"]
  key_links:
    - from: "src/lib/strava/crypto.ts"
      to: "process.env.TOKEN_ENC_KEY"
      via: "crypto.subtle.importKey on base64-decoded key"
      pattern: "importKey"
    - from: "src/lib/db/queries.ts"
      to: "stravaConnections"
      via: "onConflictDoUpdate on userId"
      pattern: "onConflictDoUpdate"
---

<objective>
Build the Phase 5 infrastructure layer that every later Strava plan depends on: the two Drizzle schema migrations (token columns on `strava_connections`, `strava_activity_id` on `training_sessions`), the AES-GCM crypto utility, the activity-matching pure function, the IDOR-safe DB query helpers, the `SessionData` CSRF field, the `TOKEN_ENC_KEY` startup null-check, the official Strava SVG asset, and the Wave 0 test scaffolds for all four testable lib modules.

Purpose: This is the dependency root for Wave 2 (API layer) and Wave 3 (UI). Tokens must never reach the DB in plaintext (STRAVA-05), so the crypto utility and the encrypted columns must exist and be proven before any token is written.
Output: 2 migrations, crypto.ts, match.ts, extended queries.ts, extended session.ts, env.ts null-check, SVG asset, 4 test files (2 implemented now, 2 scaffolded as failing stubs for Wave 2/3 modules).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-strava-integration/05-CONTEXT.md
@.planning/phases/05-strava-integration/05-RESEARCH.md
@src/lib/db/schema.ts
@src/lib/db/queries.ts
@src/lib/session.ts
@src/env.ts
@drizzle.config.ts
@drizzle/0002_quick_thunderbolt_ross.sql
</context>

<artifacts_this_phase_produces>
This plan creates the following symbols consumed by later plans:
- `encryptToken(plaintext: string): Promise<string>` — src/lib/strava/crypto.ts
- `decryptToken(stored: string): Promise<string>` — src/lib/strava/crypto.ts
- `matchActivities(activities: StravaActivity[], sessions: TrainingSession[]): Array<{ sessionId: string; stravaActivityId: bigint }>` — src/lib/strava/match.ts
- Exported types `StravaActivity` ({ id: number; start_date: string; elapsed_time: number }) and `MatchTargetSession` ({ id: string; createdAt: Date; totalDurationSec: number }) — src/lib/strava/match.ts
- `findStravaConnectionByUserId(userId)`, `upsertStravaConnection(userId, fields)`, `deleteStravaConnection(userId)`, `updateStravaTokens(userId, { accessToken, refreshToken, expiresAt })`, `updateSessionStravaMatch(userId, sessionId, stravaActivityId)` — src/lib/db/queries.ts
- `SessionData.pending_strava_state?: string` — src/lib/session.ts
- New `stravaConnections` columns: `stravaAthleteId` (bigint), `accessToken` (text), `refreshToken` (text), `expiresAt` (integer), `scope` (text), `athleteName` (text)
- New `trainingSessions` column: `stravaActivityId` (bigint, nullable)
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AES-GCM crypto utility + env null-check + SessionData field</name>
  <files>src/lib/strava/crypto.ts, src/lib/strava/crypto.test.ts, src/env.ts, src/lib/session.ts</files>
  <read_first>
    - .planning/phases/05-strava-integration/05-RESEARCH.md (Pattern 1, Pattern 10, Pitfall 5)
    - src/env.ts (existing TOKEN_ENC_KEY export at line 42; match the ANTHROPIC_API_KEY comment style)
    - src/lib/session.ts (SessionData interface at lines 21-24)
  </read_first>
  <behavior>
    - encryptToken("hello") then decryptToken(result) === "hello" (round-trip)
    - encryptToken("hello") !== "hello" (output is base64 ciphertext, not plaintext)
    - Two calls encryptToken("hello") produce different outputs (random 16-byte IV per call)
    - decryptToken of a tampered/garbage base64 string rejects (throws) — AES-GCM auth tag failure
    - Round-trips a long token-shaped string (40+ chars) and a UTF-8 string correctly
  </behavior>
  <action>
    Create src/lib/strava/crypto.ts implementing AES-GCM via Web Crypto `crypto.subtle` per RESEARCH Pattern 1 (D-05, D-06, STRAVA-05). Constants: 256-bit AES-GCM, 16-byte IV. importKey from `Buffer.from(process.env.TOKEN_ENC_KEY!, "base64")` — cache the resolved CryptoKey in a module-scope promise singleton (RESEARCH anti-pattern: do not importKey on every call). encryptToken: generate IV via `crypto.getRandomValues(new Uint8Array(16))`, encode plaintext with TextEncoder, encrypt, then return `base64(iv ++ ciphertext)` (IV prefixed, D-06). decryptToken: base64-decode, first 16 bytes = IV, remainder = ciphertext, decrypt, TextDecoder. Do NOT add `import crypto` — `crypto.subtle` and `crypto.getRandomValues` are Node 20 globals; use `Buffer` for base64 (already a Node global).
    In src/env.ts, add a startup validation immediately after the TOKEN_ENC_KEY export (Pitfall 5): if TOKEN_ENC_KEY is set but `Buffer.from(TOKEN_ENC_KEY, "base64").length !== 32`, throw an Error naming the variable and the `openssl rand -base64 32` fix. Guard so a missing var during build does not crash unrelated tooling — only validate when the value is present (mirror the conditional tone of existing exports; do not validate undefined to allow build-time absence). Keep the existing export line.
    In src/lib/session.ts, add `pending_strava_state?: string;` to the SessionData interface with a one-line comment "OAuth CSRF — set on connect, deleted after callback validation (D-01)" (Pattern 10).
  </action>
  <verify>
    <automated>npx vitest run src/lib/strava/crypto.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/strava/crypto.test.ts` passes with at least the 5 behavior cases above
    - `grep -n "pending_strava_state" src/lib/session.ts` returns a match inside the SessionData interface
    - `grep -n "byteLength\|\.length !== 32" src/env.ts` shows a 32-byte length check on TOKEN_ENC_KEY
    - `grep -c "import crypto" src/lib/strava/crypto.ts` returns 0 (uses global crypto.subtle)
    - crypto.ts contains `importKey` and `AES-GCM` (grep, filtering comments: `grep -v '^[[:space:]]*\*' src/lib/strava/crypto.ts | grep -c "AES-GCM"` ≥ 1)
  </acceptance_criteria>
  <done>encryptToken/decryptToken round-trip and reject tampered input; SessionData carries pending_strava_state; env throws on a non-32-byte TOKEN_ENC_KEY.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Activity matcher + Wave 0 test scaffolds for client/tss-chart</name>
  <files>src/lib/strava/match.ts, src/lib/strava/match.test.ts, src/lib/strava/client.test.ts, src/lib/strava/tss-chart.test.ts</files>
  <read_first>
    - .planning/phases/05-strava-integration/05-RESEARCH.md (Pattern 6, Pattern 5, Pattern 7, Pitfall 6, Validation Architecture → Wave 0 Gaps)
    - src/lib/training/tss.ts (existing computeTSS signature — buildWeeklyTSS will call it in Wave 3)
  </read_first>
  <behavior>
    - matchActivities returns { sessionId, stravaActivityId: bigint } when activity start_date UTC calendar day equals session.createdAt UTC calendar day AND elapsed_time is within [0.8x, 1.2x] of totalDurationSec
    - Returns no match when the calendar dates differ (even if duration matches)
    - Returns no match when elapsed_time is below 80% or above 120% of totalDurationSec
    - Uses elapsed_time (NOT moving_time) — test fixture includes both fields with moving_time out of range and elapsed_time in range, expects a match (Pitfall 6)
    - UTC date comparison: a session created near midnight and an activity on the next local day but same UTC day still matches (uses .toISOString().slice(0,10) on both)
  </behavior>
  <action>
    Create src/lib/strava/match.ts exporting `matchActivities(activities, sessions)` per RESEARCH Pattern 6 (D-08). Export types `StravaActivity = { id: number; start_date: string; elapsed_time: number }` and `MatchTargetSession = { id: string; createdAt: Date; totalDurationSec: number }`. For each session compute the UTC date string via `.toISOString().slice(0, 10)`; window = totalDurationSec * 0.8 to * 1.2; find the first activity whose UTC start date equals the session date AND `elapsed_time` is in-window; on hit push `{ sessionId: session.id, stravaActivityId: BigInt(matched.id) }`. Match against elapsed_time only (Pitfall 6).
    Create src/lib/strava/match.test.ts covering the 5 behavior cases with fixtures.
    Create Wave 0 failing-stub test files for modules built in later waves (Validation Architecture → Wave 0 Gaps): src/lib/strava/client.test.ts (describe blocks for `fetchWithBackoff` 429 retry behavior and `getValidAccessToken` proactive refresh — use `it.todo(...)` or a skipped test that documents the expected behavior so the file exists and runs green-but-pending), and src/lib/strava/tss-chart.test.ts (describe blocks for `buildWeeklyTSS` bucketing — `it.todo(...)`). These scaffolds make the Nyquist automated-verify path real for Wave 2/3 without producing red failures on this plan. Add a top-of-file comment in each scaffold: "Wave 0 scaffold — implement when {module} lands in Wave {N}".
  </action>
  <verify>
    <automated>npx vitest run src/lib/strava/match.test.ts src/lib/strava/client.test.ts src/lib/strava/tss-chart.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/lib/strava/match.test.ts` passes all 5 behavior cases
    - `grep -c "moving_time" src/lib/strava/match.ts` returns 0 (matcher never references moving_time)
    - `grep -c "elapsed_time" src/lib/strava/match.ts` ≥ 1
    - `grep -c "toISOString().slice(0, 10)" src/lib/strava/match.ts` ≥ 1 (UTC date comparison on both sides)
    - client.test.ts and tss-chart.test.ts exist and run without failing (it.todo/skipped scaffolds)
    - `grep -c "it.todo\|describe.skip\|it.skip" src/lib/strava/client.test.ts` ≥ 1
  </acceptance_criteria>
  <done>matchActivities passes all date/duration cases using elapsed_time + UTC dates; client/tss-chart scaffolds exist and run green-pending.</done>
</task>

<task type="auto">
  <name>Task 3: Schema migrations + IDOR-safe Strava query helpers</name>
  <files>src/lib/db/schema.ts, drizzle/0003_strava_token_columns.sql, drizzle/0004_session_strava_activity.sql, src/lib/db/queries.ts</files>
  <read_first>
    - src/lib/db/schema.ts (stravaConnections lines 86-99, trainingSessions lines 60-78 — match column ordering: FK first, domain columns, timestamps last)
    - src/lib/db/queries.ts (existing IDOR and() pattern; existing findStravaConnection at lines 124-139 — extend, do not replace)
    - drizzle/0002_quick_thunderbolt_ross.sql (ALTER TABLE ADD COLUMN format with --> statement-breakpoint)
    - .planning/phases/05-strava-integration/05-RESEARCH.md (Pattern 9, Pitfall 7 onConflictDoUpdate, Pitfall 2 BigInt)
  </read_first>
  <action>
    Schema (src/lib/db/schema.ts): import `bigint` from drizzle-orm/pg-core. To stravaConnections add (D-04): `stravaAthleteId: bigint("strava_athlete_id", { mode: "bigint" }).notNull()`, `accessToken: text("access_token").notNull()`, `refreshToken: text("refresh_token").notNull()`, `expiresAt: integer("expires_at").notNull()`, `scope: text("scope").notNull()`, `athleteName: text("athlete_name").notNull()`. Place after userId, before createdAt. To trainingSessions add (D-09): `stravaActivityId: bigint("strava_activity_id", { mode: "bigint" })` (nullable) after rawJson, before createdAt.
    Migrations: generate with `DATABASE_URL_UNPOOLED=<value> npx drizzle-kit generate`. This must produce 0003 (strava_connections ADD COLUMNs) and 0004 (training_sessions ADD COLUMN strava_activity_id). Note: adding NOT NULL columns to a table with existing rows fails if rows exist — strava_connections is empty in v1 (no connections yet), so NOT NULL is safe; if generate produces a single combined migration, that is acceptable as long as both column sets exist. Rename the generated files to the documented names if drizzle uses random suffixes is NOT required — keep drizzle's generated names but confirm the SQL contains both column sets. Run the migration against the DB: `DATABASE_URL_UNPOOLED=<value> npx drizzle-kit push` (per planning context push command). [BLOCKING] this push MUST complete before any Wave 2/3 code executes.
    Queries (src/lib/db/queries.ts) — extend, keep existing findStravaConnection. Add: `findStravaConnectionByUserId(userId)` — single eq() on userId (exempt from and() rule, same rationale as findUserProfileByUserId: userId IS the resource identifier, .unique() constraint), returns row or null. `upsertStravaConnection(userId, fields)` — `db.insert(stravaConnections).values({ userId, ...fields }).onConflictDoUpdate({ target: stravaConnections.userId, set: fields })` (Pitfall 7). `deleteStravaConnection(userId)` — `db.delete(stravaConnections).where(eq(stravaConnections.userId, userId))` (STRAVA-02). `updateStravaTokens(userId, { accessToken, refreshToken, expiresAt })` — update by userId. `updateSessionStravaMatch(userId, sessionId, stravaActivityId)` — `db.update(trainingSessions).set({ stravaActivityId }).where(and(eq(trainingSessions.userId, userId), eq(trainingSessions.id, sessionId)))` — and() MANDATORY here (secondary resource id). Add JSDoc on each noting the IDOR rule applied or the single-eq exemption, matching existing file style.
  </action>
  <verify>
    <automated>DATABASE_URL_UNPOOLED="$DATABASE_URL_UNPOOLED" npx drizzle-kit generate && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "strava_athlete_id" drizzle/*.sql` ≥ 1 and `grep -c "strava_activity_id" drizzle/*.sql` ≥ 1
    - `grep -c 'bigint(' src/lib/db/schema.ts` ≥ 2 (athlete id + activity id)
    - `grep -c "onConflictDoUpdate" src/lib/db/queries.ts` ≥ 1 (upsert on userId)
    - updateSessionStravaMatch uses `and(eq(trainingSessions.userId` (grep returns a match) — IDOR guard present
    - `npx tsc --noEmit` reports no type errors in schema.ts or queries.ts
    - drizzle-kit push completed against the DB (operator confirms in checkpoint)
  </acceptance_criteria>
  <done>Both schema migrations exist with the required columns, the DB is migrated, and all 5 Strava query helpers compile with the correct IDOR guards.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <what-built>The crypto utility, matcher, schema migrations, and query helpers are written and unit-tested. The Drizzle migration (0003/0004) needs to be pushed to the Neon database, and the four Strava env vars must be confirmed present, before Wave 2 (OAuth callback + token storage) can run.</what-built>
  <how-to-verify>
    1. Confirm STRAVA_CLIENT_ID, STRAVA_CLIENT_SECRET, TOKEN_ENC_KEY, and APP_BASE_URL are set in the local .env and in Vercel project env. TOKEN_ENC_KEY must be 32 bytes: `node -e "console.log(Buffer.from(process.env.TOKEN_ENC_KEY,'base64').length)"` prints 32.
    2. Confirm the Strava API Application's Authorization Callback Domain matches the APP_BASE_URL host.
    3. Run the migration: `DATABASE_URL_UNPOOLED=<direct-url> npx drizzle-kit push` — confirm it reports the strava_connections and training_sessions column additions applied with no errors.
  </how-to-verify>
  <resume-signal>Type "approved" once env vars are confirmed and the migration is pushed, or describe any error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Neon DB | Strava tokens cross here; must be ciphertext only |
| process.env → app | TOKEN_ENC_KEY / Strava secrets enter here; never forwarded to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | Strava tokens at rest in strava_connections | mitigate | accessToken/refreshToken columns only ever written via encryptToken() (AES-256-GCM, random IV); plaintext write is forbidden (STRAVA-05) |
| T-05-02 | Tampering | Stored ciphertext modified in DB | mitigate | AES-GCM auth tag — decryptToken throws on tampered ciphertext; caller treats as disconnected |
| T-05-03 | Elevation of Privilege | updateSessionStravaMatch / findStravaConnectionByUserId (IDOR) | mitigate | and(eq(userId), eq(id)) on resource-id queries; single eq(userId) only where userId is the sole identity (unique constraint) |
| T-05-04 | Information Disclosure | TOKEN_ENC_KEY weak/short key | mitigate | env.ts throws unless key decodes to exactly 32 bytes (Pitfall 5) |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan — slopcheck N/A; recharts install + legitimacy checkpoint handled in 05-03 |
</threat_model>

<verification>
- `npx vitest run src/lib/strava/` — crypto + match green; client/tss-chart scaffolds green-pending
- `npx tsc --noEmit` — no type errors
- `grep -rc "encryptToken\|decryptToken" src/lib/strava/crypto.ts` — both exported
- Migration SQL files contain both new column sets
- DB migration pushed (checkpoint-confirmed)
</verification>

<success_criteria>
- AES-GCM round-trip proven; tampered input rejected (STRAVA-05)
- matchActivities passes all date/duration cases using elapsed_time + UTC dates (STRAVA-03)
- Both migrations exist and are pushed; schema has all D-04/D-09 columns
- All 5 query helpers compile with correct IDOR guards (STRAVA-02 delete, match update)
- SessionData carries pending_strava_state (D-01); env throws on bad key
- Strava env vars confirmed present (checkpoint)
</success_criteria>

<output>
Create `.planning/phases/05-strava-integration/05-01-SUMMARY.md` when done
</output>
