---
phase: 05-activity-upload
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - src/lib/db/schema.ts
  - drizzle/0003_activity_uploads.sql
  - drizzle/meta/_journal.json
  - src/lib/db/queries.ts
  - src/lib/fit/tss.ts
  - src/lib/fit/match.ts
  - src/lib/fit/tss.test.ts
  - src/lib/fit/match.test.ts
  - src/lib/fit/parse.test.ts
  - src/lib/fit/tss-chart-data.test.ts
autonomous: false
requirements: [UPLOAD-01, UPLOAD-02, UPLOAD-03, PROG-02]
user_setup: []

must_haves:
  truths:
    - "activity_uploads table exists with userId FK, matchedSessionId FK (ON DELETE SET NULL), and a userId index"
    - "strava_connections table is dropped"
    - "Upload query helpers are IDOR-safe via the single and() pattern"
    - "estimateRideTSS returns a TSS integer from durationSec, avgPowerW, and ftp using IF squared"
    - "matchActivity returns the matching session by same UTC day and duration within ±20%, else null"
    - "vitest runs and the fit unit tests are present (some RED until Wave 2 implements parse)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "activityUploads table; stravaConnections removed"
      contains: "activityUploads"
    - path: "drizzle/0003_activity_uploads.sql"
      provides: "DROP strava_connections + CREATE activity_uploads migration"
      contains: "CREATE TABLE \"activity_uploads\""
    - path: "src/lib/db/queries.ts"
      provides: "insertActivityUpload, findActivityUploadsByUserId, deleteActivityUpload, setUploadMatch helpers"
      contains: "activityUploads"
    - path: "src/lib/fit/tss.ts"
      provides: "estimateRideTSS pure function"
      exports: ["estimateRideTSS"]
    - path: "src/lib/fit/match.ts"
      provides: "matchActivity pure function + FitSession/MatchableSession interfaces"
      exports: ["matchActivity"]
    - path: "vitest.config.ts"
      provides: "vitest config so test files run"
  key_links:
    - from: "src/lib/db/queries.ts"
      to: "src/lib/db/schema.ts"
      via: "import { activityUploads }"
      pattern: "import.*activityUploads.*schema"
    - from: "src/lib/fit/match.ts"
      to: "activity matcher consumers (Route Handler)"
      via: "exported matchActivity signature"
      pattern: "export function matchActivity"
---

<objective>
Lay the Wave 1 infrastructure for .fit file upload: replace the `stravaConnections` skeleton table with `activity_uploads`, generate and apply the Drizzle migration, add IDOR-safe upload query helpers, and create the two pure functions the API layer will call — `estimateRideTSS()` and `matchActivity()`. Also stand up vitest (no config exists yet) and create the four fit test files so the Nyquist gate is satisfied from Wave 1 onward.

Purpose: Wave 2 (the upload Route Handler) and Wave 3 (the chart) both depend on this schema, these helpers, and these contracts. Defining the interfaces here prevents the executor of later waves from reverse-engineering them.

Output: Updated schema + one migration file (DROP + CREATE), extended `queries.ts`, `src/lib/fit/tss.ts`, `src/lib/fit/match.ts`, `vitest.config.ts`, and four test files under `src/lib/fit/`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-strava-integration/05-CONTEXT.md
@.planning/phases/05-strava-integration/05-RESEARCH.md

@src/lib/db/schema.ts
@src/lib/db/queries.ts
@src/lib/training/tss.ts
@drizzle/0002_quick_thunderbolt_ross.sql
</context>

<interface_context>
Contracts later waves depend on — define them exactly as written here:

```typescript
// src/lib/fit/tss.ts
// Ride TSS (actual ride, NOT the planned-session computeTSS in src/lib/training/tss.ts).
// Formula (RESEARCH Pattern 4 + key_facts #4): TSS = (durationSec * IF^2 * 100) / 3600, IF = avgPowerW / ftp
export function estimateRideTSS(
  durationSec: number,
  avgPowerW: number,
  ftp: number
): number;

// src/lib/fit/match.ts
export interface FitSession {
  startedAt: Date;
  durationSec: number;
}
export interface MatchableSession {
  id: string;
  title: string;
  createdAt: Date;
  totalDurationSec: number;
}
export function matchActivity(
  fit: FitSession,
  sessions: MatchableSession[]
): { id: string; title: string } | null;
```

Schema shape (D-04):
- `activity_uploads`: id uuid PK, userId uuid FK→users (cascade), fileName text NOT NULL, startedAt timestamp NOT NULL, durationSec integer NOT NULL, avgPowerW integer nullable, estimatedTss integer nullable, matchedSessionId uuid nullable FK→training_sessions (SET NULL), createdAt timestamp default now NOT NULL; index on userId.
</interface_context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Stand up vitest + estimateRideTSS + matchActivity with unit tests</name>
  <files>vitest.config.ts, src/lib/fit/tss.ts, src/lib/fit/match.ts, src/lib/fit/tss.test.ts, src/lib/fit/match.test.ts, src/lib/fit/parse.test.ts, src/lib/fit/tss-chart-data.test.ts</files>
  <behavior>
    estimateRideTSS:
    - estimateRideTSS(3600, 200, 250) → IF=0.8, TSS = round(3600 * 0.64 * 100 / 3600) = 64
    - estimateRideTSS(1800, 250, 250) → IF=1.0, TSS = round(1800 * 1 * 100 / 3600) = 50
    matchActivity:
    - Returns {id,title} when a session has the same UTC date AND ratio durationSec/totalDurationSec is within [0.8, 1.2]
    - Returns null when the only candidate is on a different UTC calendar date
    - Returns null when the date matches but duration ratio is outside ±20% (e.g. ratio 0.5 or 1.5)
    - Date comparison uses startedAt.toISOString().slice(0,10) vs createdAt.toISOString().slice(0,10) (timezone-safe — RESEARCH anti-pattern)
  </behavior>
  <action>
    No vitest config exists yet — create `vitest.config.ts` at repo root using `@vitejs/plugin-react` (already in devDependencies), test environment `node`, and `globals: true`. Add a path alias for `@` → `./src` so test imports resolve.

    Create `src/lib/fit/tss.ts` exporting `estimateRideTSS(durationSec, avgPowerW, ftp): number` per the interface_context signature. Use the formula from key_facts #4 / RESEARCH Pattern 4: `IF = avgPowerW / ftp; return Math.round((durationSec * IF * IF * 100) / 3600)`. This is the ride/actual TSS — do NOT modify or import `computeTSS` in `src/lib/training/tss.ts` (it operates on Block arrays for planned sessions; RESEARCH anti-pattern "Modifying computeTSS"). Header comment must cite D-08.

    Create `src/lib/fit/match.ts` exporting the `FitSession`, `MatchableSession` interfaces and `matchActivity()` per interface_context. Algorithm per D-10: compare UTC date strings via `.toISOString().slice(0, 10)`; for same-day candidates compute `ratio = fit.durationSec / s.totalDurationSec` and return `{ id, title }` for the first session with `ratio >= 0.8 && ratio <= 1.2`; return null otherwise. Header comment must cite D-10.

    Create `src/lib/fit/tss.test.ts` and `src/lib/fit/match.test.ts` covering the behavior cases above. These two must be GREEN at the end of this task.

    Create the Wave 0 test scaffolds the API layer will fill: `src/lib/fit/parse.test.ts` and `src/lib/fit/tss-chart-data.test.ts`. Per the Nyquist rule, since `parseFitFile` (Wave 2) and `buildWeeklyTSS` (Wave 3) do not exist yet, scaffold each with a single `it.todo(...)` (or a skipped test) describing the expected behavior — these are RED/pending placeholders that Wave 2/Wave 3 convert to real tests. Do NOT leave them empty.
  </action>
  <verify>
    <automated>npx vitest run src/lib/fit/tss.test.ts src/lib/fit/match.test.ts --reporter=verbose</automated>
  </verify>
  <done>vitest.config.ts resolves the `@` alias; tss.test.ts and match.test.ts pass; parse.test.ts and tss-chart-data.test.ts exist as todo/skipped scaffolds; estimateRideTSS and matchActivity exported with the exact signatures in interface_context.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stravaConnections with activityUploads in schema + queries; generate migration</name>
  <files>src/lib/db/schema.ts, src/lib/db/queries.ts, drizzle/0003_activity_uploads.sql, drizzle/meta/_journal.json</files>
  <action>
    In `src/lib/db/schema.ts` (D-04): remove the entire `stravaConnections` pgTable export and replace it with `activityUploads`. Columns in order — FK first, domain columns, FK reference, timestamps last (match the project's column-ordering convention seen in trainingSessions):
    - `id` uuid PK defaultRandom
    - `userId` uuid NOT NULL references users.id `{ onDelete: "cascade" }`
    - `fileName` text NOT NULL
    - `startedAt` timestamp NOT NULL
    - `durationSec` integer NOT NULL
    - `avgPowerW` integer (nullable — no power meter; Pitfall 3)
    - `estimatedTss` integer (nullable — no FTP or no power)
    - `matchedSessionId` uuid references trainingSessions.id `{ onDelete: "set null" }` (D-04, key_facts #6 — NOT cascade)
    - `createdAt` timestamp defaultNow NOT NULL
    - index `activity_uploads_user_id_idx` on userId
    Keep the file header IDOR/and() guidance intact; add a short comment block above activityUploads citing D-04 and the SET NULL rationale (D-05: match owned here, no back-reference on training_sessions).

    In `src/lib/db/queries.ts`: remove the `stravaConnections` import and delete `findStravaConnection` (it references the dropped table and will break the TS compile — RESEARCH Runtime State Inventory). Import `activityUploads` instead and add four helpers, all in an "Activity Uploads" section mirroring the existing comment style:
    - `insertActivityUpload(values)` — `db.insert(activityUploads).values(values).returning()`, returns the inserted row; `values` typed via `typeof activityUploads.$inferInsert` (userId always supplied by caller from session).
    - `findActivityUploadsByUserId(userId)` — `db.select().from(activityUploads).where(eq(activityUploads.userId, userId))` (single eq() is correct — list query, no secondary resource id; same exemption documented for listTrainingSessions).
    - `deleteActivityUpload(userId, uploadId)` — `db.delete(activityUploads).where(and(eq(activityUploads.userId, userId), eq(activityUploads.id, uploadId))).returning()`. IDOR guard: and() is mandatory — single call (Pitfall 4). Return the deleted rows so the caller can detect a no-op (cross-user / not-found) and respond 404.
    - `setUploadMatch(userId, uploadId, matchedSessionId)` — `db.update(activityUploads).set({ matchedSessionId }).where(and(eq(activityUploads.userId, userId), eq(activityUploads.id, uploadId)))`. IDOR guard: and() single call. (Optional helper for re-match; the upload row is created with matchedSessionId already set in Wave 2, so this is for completeness/D-06 last-upload-wins re-match.)
    Add `import { and, eq } from "drizzle-orm"` is already present — ensure `and` and `eq` remain imported.

    Generate the migration: run `npx drizzle-kit generate`. Inspect the produced SQL file — it must contain `DROP TABLE "strava_connections"` AND `CREATE TABLE "activity_uploads"` with both FK constraints (userId cascade, matched_session_id SET NULL) and the userId index, matching the expected SQL in RESEARCH Pattern 6. If drizzle-kit names the file differently, that is fine — record the actual filename in the SUMMARY (this plan's files_modified lists the expected `0003_activity_uploads.sql` name).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'CREATE TABLE "activity_uploads"' drizzle/0003_*.sql && grep -q 'DROP TABLE "strava_connections"' drizzle/0003_*.sql && grep -q 'ON DELETE set null' drizzle/0003_*.sql</automated>
  </verify>
  <done>schema.ts has activityUploads and no stravaConnections; queries.ts compiles with the four upload helpers and no stravaConnections reference; one migration file exists with DROP strava_connections + CREATE activity_uploads + SET NULL FK; `npx tsc --noEmit` is clean.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>schema.ts now defines activity_uploads (and drops strava_connections); a Drizzle migration file has been generated. The migration needs to be applied to the Neon database, which requires DATABASE_URL_UNPOOLED and is a destructive (DROP TABLE) operation — Claude cannot safely run this without confirming the target environment and that strava_connections holds no data.</what-built>
  <how-to-verify>
    1. Confirm the generated `drizzle/0003_*.sql` looks correct (DROP strava_connections, CREATE activity_uploads, both FKs, userId index).
    2. Safety check (RESEARCH A3 / Runtime State Inventory): confirm `strava_connections` has no rows you care about — it is a Phase 0 skeleton table, never written to. If you want, run `SELECT COUNT(*) FROM strava_connections;` against your DB; expect 0.
    3. Apply the migration: `npx drizzle-kit migrate` (uses DATABASE_URL_UNPOOLED for migrations per CLAUDE.md).
    4. Verify the new table exists: `\d activity_uploads` in psql, or confirm via Neon console.
  </how-to-verify>
  <resume-signal>Type "migrated" once the migration is applied and activity_uploads exists, or describe any error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → query helpers | userId always supplied by the Route Handler/Action from the iron-session cookie, never from client input |
| app → database | Drizzle queries; user-scoped rows must be isolated per-user |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | deleteActivityUpload / setUploadMatch | mitigate | IDOR guard via single `and(eq(userId), eq(id))` call; never chained .where() (Pitfall 4); delete returns rows so a cross-user no-op is detectable → 404 |
| T-05-02 | Information Disclosure | findActivityUploadsByUserId | mitigate | Query filtered by `eq(activityUploads.userId, userId)`; list scoped to the caller's own rows only |
| T-05-03 | Tampering | activity_uploads FK to training_sessions | accept | `ON DELETE SET NULL` chosen deliberately (D-04) — deleting a planned session must not cascade-delete the ride record; match simply unlinks |
</threat_model>

<verification>
- `npx vitest run src/lib/fit` — tss.test.ts and match.test.ts green; parse/tss-chart-data scaffolds present (todo/skipped)
- `npx tsc --noEmit` clean (proves queries.ts no longer references the dropped table)
- migration file contains DROP strava_connections + CREATE activity_uploads + SET NULL FK + userId index
- after checkpoint: `activity_uploads` exists in the database
</verification>

<success_criteria>
- estimateRideTSS and matchActivity exist with the exact interface_context signatures and pass their unit tests
- schema.ts defines activity_uploads (D-04) and no longer defines strava_connections
- queries.ts exposes insertActivityUpload / findActivityUploadsByUserId / deleteActivityUpload / setUploadMatch, all IDOR-safe
- one Drizzle migration drops strava_connections and creates activity_uploads with the correct FKs and index
- vitest runs (config created); four fit test files exist
</success_criteria>

<output>
Create `.planning/phases/05-strava-integration/05-01-SUMMARY.md` when done. Record the actual migration filename drizzle-kit produced.
</output>
