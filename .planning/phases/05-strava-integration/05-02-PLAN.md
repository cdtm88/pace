---
phase: 05-activity-upload
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/fit/parse.ts
  - src/lib/fit/parse.test.ts
  - src/app/api/fit/upload/route.ts
  - src/lib/actions/fit.ts
  - next.config.ts
autonomous: true
requirements: [UPLOAD-01, UPLOAD-02, UPLOAD-03]
user_setup: []

must_haves:
  truths:
    - "POST /api/fit/upload parses a multipart .fit upload server-side and stores an activity_uploads row scoped to the session user"
    - "Unauthenticated upload requests get 401; files over 4MB get 413; non-FIT binaries get 400 invalid_fit_file"
    - "Each upload is matched to a training session by same UTC day + duration ±20% and the result is returned to the client"
    - "TSS is estimated and stored when both avgPowerW and the user's ftp are present, null otherwise"
    - "deleteUploadAction removes an upload row scoped to the session user (IDOR-safe)"
  artifacts:
    - path: "src/lib/fit/parse.ts"
      provides: "parseFitFile(buffer) → FitSession using fit-file-parser v3 parseAsync, mode list"
      exports: ["parseFitFile"]
    - path: "src/app/api/fit/upload/route.ts"
      provides: "POST Route Handler: auth, size check, parse, match, TSS, insert, JSON response"
      exports: ["POST"]
    - path: "src/lib/actions/fit.ts"
      provides: "deleteUploadAction Server Action (IDOR-guarded)"
      exports: ["deleteUploadAction"]
  key_links:
    - from: "src/app/api/fit/upload/route.ts"
      to: "src/lib/fit/parse.ts"
      via: "import { parseFitFile }"
      pattern: "import.*parseFitFile"
    - from: "src/app/api/fit/upload/route.ts"
      to: "src/lib/fit/match.ts + src/lib/fit/tss.ts + src/lib/db/queries.ts"
      via: "matchActivity, estimateRideTSS, insertActivityUpload"
      pattern: "insertActivityUpload"
    - from: "src/lib/actions/fit.ts"
      to: "src/lib/db/queries.ts"
      via: "deleteActivityUpload"
      pattern: "deleteActivityUpload"
---

<objective>
Build the API layer for .fit upload: install `fit-file-parser`, wrap it in `parseFitFile()`, implement the POST Route Handler at `/api/fit/upload` (auth → size check → parse → match → TSS → insert → JSON), and the `deleteUploadAction` Server Action. This closes UPLOAD-01/02/03 server-side; Wave 3 wires the UI to these endpoints.

Purpose: All binary parsing and DB writes are server-side (RESEARCH Architectural Responsibility Map — fit-file-parser is Node-only; the client never sees the raw buffer after upload). The Route Handler is the single IDOR-guarded ingestion point.

Output: `package.json` (+ fit-file-parser), `src/lib/fit/parse.ts` + filled `parse.test.ts`, `src/app/api/fit/upload/route.ts`, `src/lib/actions/fit.ts`, and possibly `next.config.ts` (only if Turbopack needs transpilePackages).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-strava-integration/05-CONTEXT.md
@.planning/phases/05-strava-integration/05-RESEARCH.md
@.planning/phases/05-strava-integration/05-01-SUMMARY.md

@src/lib/session.ts
@src/lib/actions/profile.ts
@src/lib/db/queries.ts
@src/lib/fit/match.ts
@src/lib/fit/tss.ts
</context>

<interface_context>
Define `parseFitFile` exactly so the Route Handler and parse.test.ts agree:

```typescript
// src/lib/fit/parse.ts
import FitParser from 'fit-file-parser';

export interface FitSession {
  startedAt: Date;
  durationSec: number;
  avgPowerW: number | null;
}

export async function parseFitFile(buffer: Buffer): Promise<FitSession>;
// Throws on invalid FIT binary or when no session message is present → caller returns 400.
```

Route Handler JSON success response shape (consumed by Wave 3 UploadFitButton):
```json
{
  "id": "uuid",
  "fileName": "ride.fit",
  "startedAt": "ISO string",
  "durationSec": 3600,
  "avgPowerW": 200,
  "estimatedTss": 64,
  "matchedSessionId": "uuid | null",
  "matchedSessionTitle": "string | null"
}
```
Error responses: `{ error: "unauthorized" }` 401, `{ error: "missing_file" }` 400, `{ error: "file_too_large" }` 413, `{ error: "invalid_fit_file" }` 400 (D-09).
</interface_context>

<tasks>

<task type="auto">
  <name>Task 1: Install fit-file-parser and implement parseFitFile with unit tests</name>
  <files>package.json, package-lock.json, src/lib/fit/parse.ts, src/lib/fit/parse.test.ts, next.config.ts</files>
  <action>
    Install the parser: `npm install fit-file-parser@3.0.2`. (RESEARCH Package Legitimacy Audit: fit-file-parser is APPROVED — the SUS flag is a "too-new latest version" false positive on a 9-year-old, 20K/wk package, github.com/jimmykane/fit-parser, no postinstall script. recharts is installed in Wave 3, not here.)

    Create `src/lib/fit/parse.ts` per interface_context (D-07, D-08). Use `new FitParser({ force: true, speedUnit: 'km/h', lengthUnit: 'm', temperatureUnit: 'celsius', elapsedRecordField: false, mode: 'list' })` then `await parser.parseAsync(buffer)`. Read the session at `data.sessions?.[0]` (mode 'list' → top-level array; Pitfall 2 — do NOT use mode 'cascade'). Map:
    - `startedAt: new Date(session.start_time)`
    - `durationSec: Math.round(session.total_elapsed_time ?? 0)`
    - `avgPowerW: session.avg_power ?? null` (Pitfall 3 — undefined → null, never 0)
    If `data.sessions?.[0]` is absent, throw (caller → 400). Do NOT set `export const runtime = 'edge'` anywhere — fit-file-parser needs Node Buffer (RESEARCH anti-pattern). Header comment cites D-07/D-08/D-09 and the mode:'list' access path.

    Turbopack note (RESEARCH A2 / Open Question 1): do NOT add transpilePackages preemptively. Only if `import FitParser from 'fit-file-parser'` fails at runtime with a module-resolution error, add `transpilePackages: ['fit-file-parser']` to `next.config.ts` (preserving the existing securityHeaders config) and note it in the SUMMARY. If no error occurs, leave next.config.ts unchanged and drop it from files_modified in the SUMMARY.

    Fill in `src/lib/fit/parse.test.ts` (the Wave 1 scaffold). Replace the todo with real assertions. Because constructing a valid synthetic FIT binary is non-trivial, cover what is deterministic without a real device file:
    - parseFitFile rejects (throws) when given a Buffer of random/garbage bytes (invalid FIT) — this exercises the D-09 error path.
    - If `fit-file-parser` ships any example .fit fixture in its package, or a tiny committed fixture is feasible, add a happy-path assertion that startedAt is a Date, durationSec is a number ≥ 0, and avgPowerW is number-or-null. If no fixture is practical, keep the happy-path field-mapping covered by a documented `it.todo` and rely on the Wave 3 browser checkpoint for end-to-end happy-path proof — but the invalid-binary rejection test MUST be a real passing test.
  </action>
  <verify>
    <automated>npx vitest run src/lib/fit/parse.test.ts --reporter=verbose && node -e "require('fit-file-parser')" 2>/dev/null || echo "esm-only-ok"</automated>
  </verify>
  <done>fit-file-parser@3.0.2 in package.json; parseFitFile exported with the interface_context signature using mode:'list'; parse.test.ts passes including the invalid-binary rejection test; no `runtime = 'edge'` set.</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/fit/upload Route Handler — auth, size, parse, match, TSS, insert</name>
  <files>src/app/api/fit/upload/route.ts</files>
  <action>
    Create `src/app/api/fit/upload/route.ts` exporting `async function POST(request: Request): Promise<NextResponse>` (D-01). Execution order (RESEARCH Pattern 2):
    1. Auth: `const session = await getIronSession<SessionData>(await cookies(), sessionOptions)`; if `!session.id` → `NextResponse.json({ error: 'unauthorized' }, { status: 401 })`. `await cookies()` is mandatory (Next.js 16). userId = session.id (NEVER from the form — matches the security contract in profile.ts/session.ts).
    2. Parse form: `try { formData = await request.formData() } catch { return 400 invalid_form_data }`. `const file = formData.get('file') as File | null`; if `!file` → 400 `missing_file`.
    3. Size check BEFORE arrayBuffer: `const MAX_BYTES = 4 * 1024 * 1024` (D-02 — 4MB, below Vercel's 4.5MB hard cap, Pitfall 1). If `file.size > MAX_BYTES` → 413 `file_too_large`.
    4. Convert: `const buffer = Buffer.from(await file.arrayBuffer())`.
    5. Parse: `try { const fit = await parseFitFile(buffer) } catch { return 400 invalid_fit_file }` (D-09). The match/TSS/insert below run inside the success path (after parse succeeds).
    6. Match: load the user's sessions via `listTrainingSessions(userId)`, map them to `MatchableSession[]` ({ id, title, createdAt, totalDurationSec }), call `matchActivity(fit, sessions)` → `{ id, title } | null` (D-10/D-11).
    7. TSS: read the user's profile via `findUserProfileByUserId(userId)`; if `fit.avgPowerW !== null && profile?.ftp` → `estimateRideTSS(fit.durationSec, fit.avgPowerW, profile.ftp)`, else `null` (D-08).
    8. Insert: `insertActivityUpload({ userId, fileName: file.name, startedAt: fit.startedAt, durationSec: fit.durationSec, avgPowerW: fit.avgPowerW, estimatedTss, matchedSessionId: match?.id ?? null })`.
    9. Respond: `NextResponse.json({ id, fileName, startedAt, durationSec, avgPowerW, estimatedTss, matchedSessionId, matchedSessionTitle: match?.title ?? null })` per interface_context.
    Wrap unexpected failures (insert error etc.) in a 500 `{ error: 'upload_failed' }`. Do not leak parser/DB internals to the client.
    Header comment cites D-01, D-02, D-08, D-09, D-10, D-11 and the IDOR/userId-from-session rule.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "4 \* 1024 \* 1024" src/app/api/fit/upload/route.ts && grep -q "getIronSession" src/app/api/fit/upload/route.ts && grep -q "parseFitFile" src/app/api/fit/upload/route.ts && grep -q "insertActivityUpload" src/app/api/fit/upload/route.ts</automated>
  </verify>
  <done>POST handler exists; auth gate returns 401, size gate 413, parse failure 400 invalid_fit_file; success path matches, estimates TSS, inserts, and returns the interface_context JSON; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: deleteUploadAction Server Action (IDOR-guarded)</name>
  <files>src/lib/actions/fit.ts</files>
  <action>
    Create `src/lib/actions/fit.ts` with `'use server'` at the top (UPLOAD-03). Export `async function deleteUploadAction(uploadId: string)`:
    1. Auth: `const session = await getIronSession<SessionData>(await cookies(), sessionOptions)`; if `!session.id` → `return { error: 'Not authenticated' }` (imperative action called via transition, mirror generateSessionAction's return-error pattern; the page auth gate already enforces redirect intent).
    2. Validate `uploadId` is a non-empty string.
    3. Delete via the Wave 1 helper: `const removed = await deleteActivityUpload(session.id, uploadId)` (IDOR guard lives in the helper's single and() call — D-04/Pitfall 4). If `removed.length === 0` (cross-user or not found) → `return { error: 'not_found' }` (no distinction between not-found and not-yours; 404-equivalent, D-09 anti-enumeration spirit).
    4. `revalidatePath('/dashboard')` so the chart and any confirmation re-render (matches saveProfileAction ordering — revalidate before any redirect; here we return instead of redirecting). Return `undefined` / `{ ok: true }` on success.
    Header comment cites UPLOAD-03, the IDOR rule, and the userId-from-session contract.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "'use server'" src/lib/actions/fit.ts && grep -q "deleteActivityUpload" src/lib/actions/fit.ts && grep -q "getIronSession" src/lib/actions/fit.ts</automated>
  </verify>
  <done>deleteUploadAction reads userId from session only, calls the IDOR-safe deleteActivityUpload helper, treats a zero-row delete as not_found, revalidates /dashboard; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /api/fit/upload | Untrusted multipart body (arbitrary binary + filename) crosses here |
| browser → deleteUploadAction | Untrusted uploadId crosses here |
| Route Handler → fit-file-parser | Untrusted binary parsed in Node runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Spoofing | POST /api/fit/upload | mitigate | getIronSession auth gate at handler entry; 401 when no session.id; userId never read from the form |
| T-05-05 | Denial of Service | oversized upload | mitigate | `file.size > 4MB` check BEFORE `arrayBuffer()` → 413 (Pitfall 1); avoids loading oversized file into memory; Vercel's own 4.5MB cap is the backstop |
| T-05-06 | Tampering | malformed FIT binary | mitigate | try/catch around parseFitFile → 400 invalid_fit_file (D-09); `force: true` tolerates minor corruption but result is validated (sessions[0] presence) |
| T-05-07 | Elevation of Privilege | deleteUploadAction | mitigate | deleteActivityUpload uses single and(eq(userId), eq(id)); zero-row delete → not_found (no cross-user delete possible) |
| T-05-08 | Information Disclosure | error responses | mitigate | Generic error codes only; parser/DB internals logged server-side, never returned |
| T-05-SC | Tampering | npm install fit-file-parser | mitigate | Package is APPROVED in RESEARCH legitimacy audit (9-yr history, no postinstall); SUS flag is a too-new-latest-version false positive |
</threat_model>

<verification>
- `npx tsc --noEmit` clean across new files
- `npx vitest run src/lib/fit/parse.test.ts` — invalid-binary rejection passes
- grep confirms: 4MB constant, getIronSession, parseFitFile, insertActivityUpload in the route; deleteActivityUpload + 'use server' in the action
- Manual (deferred to Wave 3 browser checkpoint): real .fit upload returns the JSON shape; >4MB returns 413; garbage file returns 400
</verification>

<success_criteria>
- fit-file-parser@3.0.2 installed; parseFitFile wraps it with mode:'list' and the interface_context signature
- POST /api/fit/upload enforces auth (401), size (413), parse (400), then matches, estimates TSS, inserts, and returns the documented JSON
- deleteUploadAction is IDOR-safe and revalidates the dashboard
- No edge runtime declared; no transpilePackages unless a runtime resolution error required it
</success_criteria>

<output>
Create `.planning/phases/05-strava-integration/05-02-SUMMARY.md` when done. Note whether transpilePackages was needed.
</output>
