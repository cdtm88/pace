---
phase: 05-activity-upload
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/copy.ts
  - src/lib/fit/tss-chart-data.ts
  - src/lib/fit/tss-chart-data.test.ts
  - src/components/fit/tss-chart.tsx
  - src/components/fit/upload-fit-button.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: false
requirements: [UPLOAD-01, UPLOAD-02, UPLOAD-03, PROG-02]
user_setup: []

must_haves:
  truths:
    - "User can pick a .fit file on the dashboard and upload it; the card shows a matched / no-match confirmation inline"
    - "User can delete an uploaded activity from the dashboard"
    - "Dashboard renders a weekly TSS bar chart (6-week rolling, orange bars) from matched uploads"
    - "Empty chart shows the centered 'Upload .fit files to see your training load' label"
    - "All upload UI strings come from src/lib/copy.ts"
  artifacts:
    - path: "src/lib/fit/tss-chart-data.ts"
      provides: "buildWeeklyTSS(uploads) → WeeklyTSSPoint[] (6 Monday-anchored buckets)"
      exports: ["buildWeeklyTSS"]
    - path: "src/components/fit/tss-chart.tsx"
      provides: "TssChart Client Component (recharts BarChart, 300px, #f97316, empty state)"
      exports: ["TssChart"]
    - path: "src/components/fit/upload-fit-button.tsx"
      provides: "UploadFitButton Client Component (file input → fetch /api/fit/upload, inline result, delete)"
      exports: ["UploadFitButton"]
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "Log a Ride card + TSS chart wired into the dashboard RSC"
      contains: "UploadFitButton"
  key_links:
    - from: "src/components/fit/upload-fit-button.tsx"
      to: "/api/fit/upload"
      via: "fetch POST multipart"
      pattern: "fetch\\(.*api/fit/upload"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/lib/fit/tss-chart-data.ts + src/components/fit/tss-chart.tsx"
      via: "buildWeeklyTSS(findActivityUploadsByUserId(...)) passed as prop to TssChart"
      pattern: "buildWeeklyTSS"
    - from: "src/components/fit/upload-fit-button.tsx"
      to: "src/lib/actions/fit.ts"
      via: "deleteUploadAction"
      pattern: "deleteUploadAction"
---

<objective>
Build the dashboard UI for .fit upload and the weekly TSS chart. Install recharts (after a legitimacy checkpoint), add upload COPY keys, implement `buildWeeklyTSS()`, the `TssChart` recharts Client Component, the `UploadFitButton` Client Component (file input → fetch → inline confirmation + delete), and wire both into the dashboard RSC below the session generator (D-12). A browser-verify checkpoint confirms the full upload→match→chart→delete loop, since this is the user-facing close of the generate → ride → log loop.

Purpose: This is the only wave the user can see. It must close PROG-02 (TSS chart) and the UPLOAD-01/02/03 user-facing surface against the Wave 2 API.

Output: recharts in package.json, upload COPY keys, `src/lib/fit/tss-chart-data.ts` + filled test, two `src/components/fit/` components, and the updated dashboard page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-strava-integration/05-CONTEXT.md
@.planning/phases/05-strava-integration/05-RESEARCH.md
@.planning/phases/05-strava-integration/05-UI-SPEC.md
@.planning/phases/05-strava-integration/05-01-SUMMARY.md
@.planning/phases/05-strava-integration/05-02-SUMMARY.md

@src/app/(app)/dashboard/page.tsx
@src/lib/copy.ts
@src/lib/db/queries.ts
@src/lib/actions/fit.ts
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/components/ui/error-banner.tsx
</context>

<interface_context>
```typescript
// src/lib/fit/tss-chart-data.ts
export interface WeeklyTSSPoint { week: string; tss: number }   // week: "Jun 9"
export interface UploadForChart {
  startedAt: Date;
  estimatedTss: number | null;
  matchedSessionId: string | null;
}
export function buildWeeklyTSS(uploads: UploadForChart[]): WeeklyTSSPoint[]; // always 6 points

// src/components/fit/tss-chart.tsx
'use client';
export function TssChart({ data }: { data: WeeklyTSSPoint[] }): JSX.Element;

// src/components/fit/upload-fit-button.tsx
'use client';
export interface RecentUpload {
  id: string; fileName: string; matchedSessionTitle: string | null;
}
export function UploadFitButton({ recentUploads }: { recentUploads: RecentUpload[] }): JSX.Element;
```

UI-SPEC is partly stale (it describes the abandoned Strava OAuth card). Authoritative for THIS phase: CONTEXT D-12..D-15. Reuse ONLY the chart visual contract from UI-SPEC that still applies: BarChart + ResponsiveContainer height 300, `#f97316` bars, radius [4,4,0,0], 12px axis ticks, X-axis week labels. Ignore all Strava OAuth / connect-button / refresh / disconnect UI-SPEC content.
</interface_context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Wave 3 needs `recharts@3.8.1` installed. recharts is APPROVED in the RESEARCH Package Legitimacy Audit (OK verdict — ~8yr history, 52M weekly downloads, github.com/recharts/recharts). Per GSD package-legitimacy policy this confirmation checkpoint runs before the install task.</what-built>
  <how-to-verify>
    1. Confirm recharts on the registry: visit https://www.npmjs.com/package/recharts — expect ~8yr age, tens of millions of weekly downloads, repo github.com/recharts/recharts.
    2. Confirm the target version `3.8.1` matches CLAUDE.md tech stack + RESEARCH.
    3. This is the only new dependency this wave (fit-file-parser was installed in Wave 2).
  </how-to-verify>
  <resume-signal>Type "approved" to allow `npm install recharts@3.8.1`, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install recharts, add upload COPY keys, implement buildWeeklyTSS</name>
  <files>package.json, package-lock.json, src/lib/copy.ts, src/lib/fit/tss-chart-data.ts, src/lib/fit/tss-chart-data.test.ts</files>
  <action>
    Install: `npm install recharts@3.8.1` (only after the checkpoint above is approved).

    Add a "Phase 5: Activity upload" section to the `COPY` object in `src/lib/copy.ts`. COPY key names are Claude's discretion (CONTEXT D-13/D-15) — define these strings (caller substitutes `{title}` tokens via .replace, matching the existing DASHBOARD_FTP_ACTIVE pattern):
    - `UPLOAD_CARD_TITLE`: "Log a Ride"
    - `UPLOAD_CARD_DESCRIPTION`: "Upload a .fit file from your Garmin, Wahoo, or Zwift ride."
    - `UPLOAD_BTN`: "Upload .fit file"
    - `UPLOAD_BTN_LOADING`: "Uploading…"
    - `UPLOAD_SUCCESS_MATCHED`: "Logged — matched to {title}" (D-13)
    - `UPLOAD_SUCCESS_UNMATCHED`: "Logged — no session matched for this date" (D-11/D-13)
    - `UPLOAD_ERROR_INVALID`: "File couldn't be read — make sure it's a .fit file" (D-09)
    - `UPLOAD_ERROR_TOO_LARGE`: "That file is over the 4 MB limit." (D-02)
    - `UPLOAD_ERROR_GENERIC`: "Upload failed. Please try again."
    - `UPLOAD_DELETE_BTN`: "Delete"
    - `CHART_TSS_TITLE`: "Training load"
    - `CHART_TSS_DESCRIPTION`: "Weekly TSS · 6-week window"
    - `CHART_TSS_EMPTY_LABEL`: "Upload .fit files to see your training load" (D-15 — verbatim)
    Keep `as const`.

    Create `src/lib/fit/tss-chart-data.ts` exporting `buildWeeklyTSS` and the interfaces per interface_context (D-14). Build exactly 6 Monday-anchored week buckets (oldest→newest) using a `getMonday(date, weekOffset)` helper (Monday = day 1, Sunday wraps to previous Monday). For each bucket, sum `estimatedTss` over uploads where `matchedSessionId !== null && estimatedTss !== null && startedAt >= weekStart && startedAt < weekEnd`. Label each bucket via `weekStart.toLocaleDateString('en-US', { month: 'short', day: 'numeric' })` → "Jun 9". Follow the reference in RESEARCH "Code Examples → buildWeeklyTSS". Header comment cites D-14.

    Fill in `src/lib/fit/tss-chart-data.test.ts` (the Wave 1 scaffold): replace the todo with real tests —
    - returns exactly 6 points
    - an upload with matchedSessionId set and estimatedTss=64 dated within the current week contributes 64 to the latest bucket
    - an upload with matchedSessionId=null (or estimatedTss=null) contributes 0 (excluded)
    - an upload older than 6 weeks contributes to none of the buckets
    Construct upload dates relative to `new Date()` so the test stays stable regardless of when it runs.
  </action>
  <verify>
    <automated>npx vitest run src/lib/fit/tss-chart-data.test.ts --reporter=verbose && grep -q "recharts" package.json && grep -q "CHART_TSS_EMPTY_LABEL" src/lib/copy.ts</automated>
  </verify>
  <done>recharts@3.8.1 in package.json; upload + chart COPY keys present (D-15 label verbatim); buildWeeklyTSS returns 6 buckets with correct matched/TSS filtering; tss-chart-data.test.ts passes.</done>
</task>

<task type="auto">
  <name>Task 2: TssChart + UploadFitButton Client Components</name>
  <files>src/components/fit/tss-chart.tsx, src/components/fit/upload-fit-button.tsx</files>
  <action>
    Create `src/components/fit/tss-chart.tsx` with `'use client'` (D-14, D-15). Props `{ data: WeeklyTSSPoint[] }`. If `data.length === 0 || data.every(d => d.tss === 0)` render a 300px-high flex-centered div with `COPY.CHART_TSS_EMPTY_LABEL` in muted text (D-15 empty state). Otherwise render `<ResponsiveContainer width="100%" height={300}>` wrapping `<BarChart data={data} margin={{ top: 8, right: 8, bottom: 8, left: 0 }}>` with `<XAxis dataKey="week" tick={{ fontSize: 12 }} />`, `<YAxis tick={{ fontSize: 12 }} />`, `<Tooltip />`, and `<Bar dataKey="tss" fill="#f97316" radius={[4, 4, 0, 0]} />`. Import COPY for the empty label. recharts v3 is SVG and safe in a Client Component — no dynamic import needed (RESEARCH Pattern 5). Add `aria-label="Weekly training load bar chart, 6-week rolling window"` on the wrapping div (UI-SPEC accessibility). Keep TssChart presentational (just the chart/empty-state); the dashboard wraps it in a Card.

    Create `src/components/fit/upload-fit-button.tsx` with `'use client'` (D-03, D-11, D-13). Props `{ recentUploads: RecentUpload[] }`. Behavior:
    - A hidden `<input type="file" accept=".fit">` triggered by a Button (reuse `@/components/ui/button`). On file change, build `FormData`, append the file under key `'file'` (MUST match `formData.get('file')` in the Route Handler), and `fetch('/api/fit/upload', { method: 'POST', body: formData })`. Do NOT set Content-Type manually — the browser sets the multipart boundary.
    - Track state: idle / uploading / success / error via useState (plus a loading boolean or useTransition). While uploading, show `COPY.UPLOAD_BTN_LOADING` and disable the control.
    - On success (res.ok): parse JSON; show inline confirmation — `COPY.UPLOAD_SUCCESS_MATCHED.replace('{title}', json.matchedSessionTitle)` when `matchedSessionId` is non-null, else `COPY.UPLOAD_SUCCESS_UNMATCHED` (D-11/D-13). Then `router.refresh()` so the RSC chart re-fetches and re-renders with the new matched upload.
    - On error: map status → COPY — 413 → `UPLOAD_ERROR_TOO_LARGE`, 400 (`invalid_fit_file`) → `UPLOAD_ERROR_INVALID`, anything else → `UPLOAD_ERROR_GENERIC`. Surface via the existing `ErrorBanner` component (`@/components/ui/error-banner`, role="alert").
    - Delete: render `recentUploads` (filename + matched title when present) each with a Delete button (`COPY.UPLOAD_DELETE_BTN`) that calls `deleteUploadAction(upload.id)` from `@/lib/actions/fit` inside a transition; on resolve `router.refresh()`. Direct delete, no confirm modal (RESEARCH Open Question 2 — accidental delete just means re-uploading; low cost).
    All user-visible text via COPY. Interactive controls sized for the mobile on-bike context (≥44px tap target where practical).
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "api/fit/upload" src/components/fit/upload-fit-button.tsx && grep -q "#f97316" src/components/fit/tss-chart.tsx && grep -q "deleteUploadAction" src/components/fit/upload-fit-button.tsx</automated>
  </verify>
  <done>TssChart renders the empty state and the orange BarChart at 300px; UploadFitButton posts multipart to /api/fit/upload, shows matched/unmatched/error inline via COPY + ErrorBanner, refreshes the RSC on success, and deletes via deleteUploadAction; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire Log a Ride card + TSS chart into the dashboard RSC</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <action>
    Edit `src/app/(app)/dashboard/page.tsx` (D-12). In the server component, after the existing profile/latestSession fetches, fetch uploads: `const uploads = await findActivityUploadsByUserId(session.id)`. Build chart data server-side: `const weeklyTSS = buildWeeklyTSS(uploads)` (each upload supplies `{ startedAt, estimatedTss, matchedSessionId }` — the row shape already matches `UploadForChart`). Build `recentUploads` for the delete list by mapping uploads to `{ id, fileName, matchedSessionTitle }`. The upload row stores `matchedSessionId` but not the title; passing `matchedSessionTitle: null` in the list is acceptable (the inline post-upload confirmation is the authoritative match display per D-13). If a title in the list is desired, resolve it in-memory against `listTrainingSessions(session.id)` — optional, do not add a new query just for this.

    Insert into the JSX, positioned per D-12 / UI-SPEC layout — below the SessionGenerator + "View session" link, ABOVE the "Edit profile" link / signed-in email / sign-out form:
    - A "Log a Ride" Card (`@/components/ui/card`): CardHeader with `COPY.UPLOAD_CARD_TITLE` + `COPY.UPLOAD_CARD_DESCRIPTION`, CardContent rendering `<UploadFitButton recentUploads={recentUploads} />`.
    - A "Training load" Card: CardHeader with `COPY.CHART_TSS_TITLE` + `COPY.CHART_TSS_DESCRIPTION`, CardContent rendering `<TssChart data={weeklyTSS} />`. The chart's parent must not collapse height — TssChart uses height={300} on ResponsiveContainer, so the card content only needs to allow it (Pitfall 5).
    Maintain the existing `space-y-6` rhythm between sections. Import COPY, both components, buildWeeklyTSS, and findActivityUploadsByUserId. tsc must stay clean.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q "UploadFitButton" "src/app/(app)/dashboard/page.tsx" && grep -q "TssChart" "src/app/(app)/dashboard/page.tsx" && grep -q "buildWeeklyTSS" "src/app/(app)/dashboard/page.tsx"</automated>
  </verify>
  <done>Dashboard renders the "Log a Ride" card (UploadFitButton) and "Training load" card (TssChart) below the session generator and above the profile/logout area; chart data is built server-side via buildWeeklyTSS over findActivityUploadsByUserId; tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>The full .fit upload loop is wired: dashboard "Log a Ride" card with file upload, server parse + match + TSS, inline confirmation, the weekly TSS bar chart, and delete. This is the user-facing close of the generate → ride → log loop and must be verified in a browser before the phase is considered done.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and sign in. Go to /dashboard.
    2. Confirm the "Log a Ride" card appears below the session generator and the "Training load" chart card below it. With no uploads yet, the chart shows the empty label "Upload .fit files to see your training load" (D-15).
    3. Upload a real .fit file from a Garmin/Wahoo/Zwift ride.
       - If it was ridden the same UTC day as a generated session with a duration within ±20%, the card shows "Logged — matched to {session title}" (D-13).
       - Otherwise it shows "Logged — no session matched for this date" (D-11).
    4. After a matched upload with power data + an FTP set on your profile, the TSS chart shows an orange bar in the current week's bucket (PROG-02). Confirm bars are `#f97316`, chart is ~300px tall, X-axis shows week labels like "Jun 9".
    5. Try a non-.fit file (e.g. rename a .txt to .fit, or pick a random binary): expect the inline error "File couldn't be read — make sure it's a .fit file" (D-09).
    6. Delete an uploaded activity from the list: it disappears and, if it was the only matched upload in a week, that bar drops to 0 (UPLOAD-03).
    7. Confirm `npm run build` completes (verifies recharts + fit-file-parser bundle under Turbopack; if a module-resolution error appears for fit-file-parser, add `transpilePackages: ['fit-file-parser']` to next.config.ts and note it).
  </how-to-verify>
  <resume-signal>Type "approved" once the upload→match→chart→delete loop works in the browser, or describe what's broken.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser file input → /api/fit/upload | Untrusted file selected by the user; sent multipart to the Wave 2 handler |
| client delete button → deleteUploadAction | Untrusted uploadId; guarded server-side in the Wave 2 action |
| server → recharts (client bundle) | Chart data is the user's own matched uploads; no cross-user data reaches the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure | dashboard RSC → TssChart prop | mitigate | weeklyTSS is built from `findActivityUploadsByUserId(session.id)` only — chart data is the caller's own uploads; no other user's rows reach the client bundle |
| T-05-10 | Tampering | upload-fit-button error mapping | accept | Client maps HTTP status → COPY message; the authoritative validation (auth/size/parse/IDOR) is all server-side in Wave 2; the client cannot bypass it |
| T-05-SC | Tampering | npm install recharts | mitigate | recharts is OK in the RESEARCH legitimacy audit; blocking-human checkpoint precedes the install |
</threat_model>

<verification>
- `npx vitest run src/lib/fit/tss-chart-data.test.ts` — buildWeeklyTSS green (6 buckets, matched/TSS filtering)
- `npx tsc --noEmit` clean across the chart-data util, both components, and the dashboard
- grep confirms: recharts in package.json; fetch to /api/fit/upload + deleteUploadAction in UploadFitButton; #f97316 in TssChart; UploadFitButton/TssChart/buildWeeklyTSS in the dashboard
- `npm run build` completes (Turbopack bundles recharts + fit-file-parser)
- Browser checkpoint: empty-state label, matched/unmatched confirmation, orange chart bar after matched upload, invalid-file error, delete removes the activity and drops the bar
</verification>

<success_criteria>
- recharts@3.8.1 installed via the approved checkpoint
- All upload/chart strings sourced from COPY (D-15 empty label verbatim)
- buildWeeklyTSS produces 6 Monday-anchored buckets from matched uploads (PROG-02)
- TssChart renders the empty state and the orange 300px BarChart
- UploadFitButton uploads to /api/fit/upload, shows matched/unmatched/error inline, refreshes the chart, and deletes via deleteUploadAction (UPLOAD-01/02/03)
- Dashboard shows the "Log a Ride" + "Training load" cards in the D-12 position; build passes; browser loop verified
</success_criteria>

<output>
Create `.planning/phases/05-strava-integration/05-03-SUMMARY.md` when done. Note whether transpilePackages was needed for the build.
</output>
