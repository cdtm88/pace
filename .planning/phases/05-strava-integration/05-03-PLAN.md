---
phase: 05-strava-integration
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - package.json
  - src/lib/copy.ts
  - src/lib/strava/tss-chart-data.ts
  - src/lib/strava/tss-chart.test.ts
  - src/components/strava/tss-chart.tsx
  - src/components/strava/strava-section.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: false
requirements: [PROG-02, STRAVA-01, STRAVA-02, STRAVA-04]

must_haves:
  truths:
    - "Disconnected dashboard shows the official Connect with Strava SVG button"
    - "Connected dashboard shows 'Connected as {athleteName}' + Refresh + Disconnect"
    - "Tapping Disconnect shows an inline two-step confirm before deleting the connection"
    - "Refresh button shows a loading state and surfaces a tap-to-retry ErrorBanner on 429"
    - "TSS chart renders 6 weekly bars (orange) when matched sessions exist; renders an empty-state label overlay when none exist"
    - "buildWeeklyTSS groups matched sessions into the correct 6 week buckets and sums TSS per bucket"
  artifacts:
    - path: "src/lib/strava/tss-chart-data.ts"
      provides: "buildWeeklyTSS(sessions, ftp) → 6 WeeklyBucket"
      exports: ["buildWeeklyTSS"]
    - path: "src/components/strava/tss-chart.tsx"
      provides: "Client recharts BarChart with empty-state overlay"
      exports: ["TSSChart"]
    - path: "src/components/strava/strava-section.tsx"
      provides: "Client connect/disconnect/refresh UI"
      exports: ["StravaSection"]
    - path: "src/lib/copy.ts"
      provides: "16 Phase 5 COPY keys"
      contains: "CHART_TSS_EMPTY_LABEL"
  key_links:
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "StravaSection + TSSChart"
      via: "RSC computes connection state + weekly buckets, passes as props"
      pattern: "buildWeeklyTSS"
    - from: "src/components/strava/strava-section.tsx"
      to: "connectStravaAction / disconnectStravaAction / refreshStravaAction"
      via: "form action + transition"
      pattern: "StravaAction"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "findStravaConnectionByUserId"
      via: "RSC connection lookup; BigInt → String before client boundary"
      pattern: "findStravaConnectionByUserId"
---

<objective>
Build the dashboard UI for Strava: install recharts, add the 16 COPY keys, implement the in-memory 6-week TSS aggregation, the recharts chart Client Component (with empty-state overlay), the Strava connection Client Component (connect SVG / connected status / refresh / inline-confirm disconnect / 429 retry), and wire all of it into the dashboard RSC below the session generator.

Purpose: This is the user-facing close of the generate → ride → log loop — the connect button, the sync, and the training-load chart (PROG-02). It consumes the Server Actions and query helpers from Waves 1-2.
Output: recharts dependency, COPY keys, tss-chart-data.ts (+ tests), tss-chart.tsx, strava-section.tsx, dashboard wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-strava-integration/05-CONTEXT.md
@.planning/phases/05-strava-integration/05-RESEARCH.md
@.planning/phases/05-strava-integration/05-UI-SPEC.md
@.planning/phases/05-strava-integration/05-01-SUMMARY.md
@.planning/phases/05-strava-integration/05-02-SUMMARY.md
@src/app/(app)/dashboard/page.tsx
@src/lib/copy.ts
@src/components/ui/error-banner.tsx
@src/components/ui/card.tsx
@src/components/ui/button.tsx
@src/lib/training/tss.ts
</context>

<artifacts_this_phase_produces>
- `buildWeeklyTSS(sessions, ftp): WeeklyBucket[]` and type `WeeklyBucket = { weekLabel: string; tss: number }` — src/lib/strava/tss-chart-data.ts
- `TSSChart({ data })` — src/components/strava/tss-chart.tsx
- `StravaSection({ connection })` — src/components/strava/strava-section.tsx
- 16 new COPY keys (see UI-SPEC §Copywriting Contract) — src/lib/copy.ts
</artifacts_this_phase_produces>

<context_for_executor>
Interfaces from Waves 1-2 you consume — do not re-derive:
- `connectStravaAction()`, `disconnectStravaAction()`, `refreshStravaAction(): Promise<{ error?: string }>` from @/lib/actions/strava (refresh returns error codes "rate_limited" | "disconnected" | "unknown")
- `findStravaConnectionByUserId(userId)`, `listTrainingSessions(userId)` from @/lib/db/queries
- `computeTSS(blocks, ftp)` from @/lib/training/tss (returns number | null)
- Connection row shape: { athleteName: string, stravaAthleteId: bigint, expiresAt: number, ... }. BigInt fields MUST be String()-converted before crossing the RSC→Client boundary (Pitfall 2).
- Session row: { id, createdAt: Date, blocks: jsonb, totalDurationSec, stravaActivityId: bigint | null }
</context_for_executor>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>recharts 3.8.1 is declared in CLAUDE.md as the chart library but is NOT currently in package.json. It must be installed before the TSS chart can be built. recharts is a widely-used charting library — this checkpoint confirms legitimacy before the install runs (package legitimacy gate; [ASSUMED] status since it is not yet in the lockfile).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/recharts — confirm it is the official Recharts package (weekly downloads in the millions, repo github.com/recharts/recharts, latest 3.x).
    2. Confirm the version to install is 3.8.1 (per CLAUDE.md tech stack).
  </how-to-verify>
  <resume-signal>Type "approved" to install recharts@3.8.1, or specify a different version.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Install recharts + COPY keys + weekly TSS aggregation</name>
  <files>package.json, src/lib/copy.ts, src/lib/strava/tss-chart-data.ts, src/lib/strava/tss-chart.test.ts</files>
  <read_first>
    - .planning/phases/05-strava-integration/05-UI-SPEC.md (§Copywriting Contract — exact 16 key names + strings)
    - .planning/phases/05-strava-integration/05-RESEARCH.md (Pattern 7 buildWeeklyTSS, Assumption A6 week boundaries)
    - src/lib/copy.ts (append to the COPY object; match existing section-comment style)
    - src/lib/training/tss.ts (computeTSS signature)
    - src/lib/strava/tss-chart.test.ts (Wave 0 scaffold from 05-01 — replace it.todo with real tests)
  </read_first>
  <behavior>
    - buildWeeklyTSS returns exactly 6 buckets, oldest-first, each with a weekLabel like "Jun 9"
    - A matched session (stravaActivityId non-null) in week N contributes computeTSS(blocks, ftp) to that bucket's tss
    - An unmatched session (stravaActivityId null) contributes 0 (excluded)
    - A week with no matched sessions has tss === 0
    - Two matched sessions in the same week sum their TSS into one bucket
    - When ftp is null, computeTSS returns null per-session → treated as 0 contribution
  </behavior>
  <action>
    Install: `npm install recharts@3.8.1` (after the legitimacy checkpoint approves).
    COPY (src/lib/copy.ts): append a "// ── Phase 5: Strava + TSS chart" section with the 16 keys EXACTLY as named in UI-SPEC §Copywriting Contract: STRAVA_SECTION_TITLE, STRAVA_DISCONNECTED_DESCRIPTION, STRAVA_CONNECTED_AS, STRAVA_BTN_REFRESH, STRAVA_BTN_REFRESH_LOADING, STRAVA_LINK_DISCONNECT, STRAVA_DISCONNECT_CONFIRM, STRAVA_DISCONNECT_CONFIRM_BTN, STRAVA_DISCONNECT_CANCEL_BTN, STRAVA_CONNECTED_SUCCESS, STRAVA_ERROR_SCOPE_DENIED, STRAVA_ERROR_RATE_LIMITED, STRAVA_BTN_RETRY, CHART_TSS_TITLE, CHART_TSS_DESCRIPTION, CHART_TSS_EMPTY_LABEL — with the verbatim strings from the UI-SPEC table. STRAVA_CONNECTED_AS keeps the `{athleteName}` token for caller .replace(). CHART_TSS_EMPTY_LABEL is "Complete sessions and connect Strava to see training load" verbatim (D-15).
    Aggregation (src/lib/strava/tss-chart-data.ts): export type `WeeklyBucket = { weekLabel: string; tss: number }` and `buildWeeklyTSS(sessions, ftp: number | null): WeeklyBucket[]` per RESEARCH Pattern 7 — Sunday-start weeks, loop w=5..0, weekStart = now - now.getDay() - w*7 at 00:00:00, weekEnd = weekStart + 7 days; label via `toLocaleDateString("en-US", { month: "short", day: "numeric" })`; sum `computeTSS(s.blocks, ftp) ?? 0` over sessions where `s.stravaActivityId != null && createdAt in [weekStart, weekEnd)`. Pure function — accept `now` as an optional injected param defaulting to `new Date()` so tests are deterministic.
    Replace the Wave 0 scaffold in tss-chart.test.ts with the 6 behavior cases, passing a fixed `now` for stable bucket boundaries.
  </action>
  <verify>
    <automated>npx vitest run src/lib/strava/tss-chart.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "recharts" package.json` ≥ 1 (dependency added)
    - `npx vitest run src/lib/strava/tss-chart.test.ts` passes all 6 behavior cases (no it.todo)
    - `grep -c "CHART_TSS_EMPTY_LABEL\|STRAVA_SECTION_TITLE\|STRAVA_CONNECTED_AS\|STRAVA_BTN_RETRY" src/lib/copy.ts` === 4 (keys present)
    - `grep -c "Complete sessions and connect Strava to see training load" src/lib/copy.ts` === 1 (D-15 verbatim)
    - buildWeeklyTSS returns 6 buckets (asserted in tests); `grep -c "stravaActivityId" src/lib/strava/tss-chart-data.ts` ≥ 1 (only matched sessions counted)
  </acceptance_criteria>
  <done>recharts installed; 16 COPY keys present with verbatim strings; buildWeeklyTSS sums matched-session TSS into 6 correct weekly buckets.</done>
</task>

<task type="auto">
  <name>Task 2: TSSChart + StravaSection Client Components</name>
  <files>src/components/strava/tss-chart.tsx, src/components/strava/strava-section.tsx</files>
  <read_first>
    - .planning/phases/05-strava-integration/05-UI-SPEC.md (§Screen Layout Contract, §States Per Screen, §Destructive Actions, §Interaction Patterns, §Accessibility Contract)
    - .planning/phases/05-strava-integration/05-RESEARCH.md (Pattern 8 recharts)
    - src/components/ui/card.tsx, src/components/ui/button.tsx, src/components/ui/error-banner.tsx (exact prop APIs)
    - src/components/session/session-generator.tsx (existing useTransition + action-call client pattern in this app)
  </read_first>
  <action>
    TSSChart (src/components/strava/tss-chart.tsx, `"use client"`) per RESEARCH Pattern 8 + UI-SPEC §TSS Chart Card: props `{ data: WeeklyBucket[] }`. Wrap in a `relative h-[300px] w-full` div. ResponsiveContainer width="100%" height={300} with `aria-label="Weekly training load bar chart, 6-week rolling window"` on the wrapper. BarChart margin { top:8, right:8, bottom:8, left:0 }; XAxis dataKey="weekLabel" tick fontSize 12; YAxis tick fontSize 12; Bar dataKey="tss" fill="#f97316" radius={[4,4,0,0]}. If `data.every(d => d.tss === 0)` render an absolute-inset centered overlay with COPY.CHART_TSS_EMPTY_LABEL in text-sm text-muted-foreground (D-15 — chart frame still renders).
    StravaSection (src/components/strava/strava-section.tsx, `"use client"`) per UI-SPEC §Strava Section Card + §States + §Destructive Actions: props `{ connection: { athleteName: string } | null }`. Use Card/CardHeader/CardTitle/CardContent.
      - Disconnected (connection null): CardTitle=COPY.STRAVA_SECTION_TITLE, CardDescription=COPY.STRAVA_DISCONNECTED_DESCRIPTION, CardContent renders a `<form action={connectStravaAction}>` whose submit control is `<img src="/connect-with-strava.svg" alt="Connect with Strava" />` (button-wrapped img, type=submit). Do not recolor or scale the SVG below 48px height.
      - Connected: CardTitle=COPY.STRAVA_SECTION_TITLE; CardAction = Refresh Button (variant="default", min-h-12 h-12, ≥48px touch target). CardContent shows `<p>` with COPY.STRAVA_CONNECTED_AS.replace("{athleteName}", connection.athleteName) and a Disconnect Button (variant="ghost", text-destructive, min-h-12).
      - Refresh interaction: useTransition; on click call refreshStravaAction(); while pending button disabled + aria-busy="true" + label COPY.STRAVA_BTN_REFRESH_LOADING + Loader2 spinner (lucide-react). On returned { error: "rate_limited" } render ErrorBanner message=COPY.STRAVA_ERROR_RATE_LIMITED plus a "Tap to retry" Button (variant="outline", label COPY.STRAVA_BTN_RETRY) that re-invokes refreshStravaAction. On { error: "disconnected" } surface COPY.STRAVA_ERROR_SCOPE_DENIED-style reconnect prompt (use a reconnect message; map "disconnected" to a reconnect ErrorBanner). On success clear any error.
      - Disconnect interaction (two-step inline, no modal — UI-SPEC §Destructive Actions): clicking Disconnect replaces the button with COPY.STRAVA_DISCONNECT_CONFIRM text + "Yes, disconnect" (variant="destructive", calls disconnectStravaAction via transition) + "Cancel" (variant="ghost", reverts). Move focus to the first confirm button on reveal (useRef + focus()).
      - No hover-only interactions (PWA-04); all controls ≥48×48 (PWA-03 applied proactively).
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - both files start with `"use client"` (grep ≥ 1 each)
    - `grep -c 'fill="#f97316"' src/components/strava/tss-chart.tsx` === 1 (orange bar, D-13)
    - `grep -c "CHART_TSS_EMPTY_LABEL" src/components/strava/tss-chart.tsx` ≥ 1 (empty overlay, D-15)
    - `grep -c "connect-with-strava.svg" src/components/strava/strava-section.tsx` ≥ 1 (official asset)
    - strava-section references connectStravaAction, disconnectStravaAction, refreshStravaAction (grep each ≥ 1)
    - `grep -c "STRAVA_DISCONNECT_CONFIRM\|STRAVA_DISCONNECT_CONFIRM_BTN" src/components/strava/strava-section.tsx` ≥ 1 (two-step confirm)
    - `grep -c "aria-busy\|aria-label" src/components/strava/strava-section.tsx src/components/strava/tss-chart.tsx` ≥ 2
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>TSSChart renders orange 6-bar chart + empty overlay; StravaSection covers disconnected/connected/refreshing/429-retry/inline-disconnect states with ≥48px touch targets and COPY strings.</done>
</task>

<task type="auto">
  <name>Task 3: Dashboard wiring + connect-success banner</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(app)/dashboard/page.tsx (existing layout: max-w-lg space-y-6; SessionGenerator placement; existing query calls)
    - .planning/phases/05-strava-integration/05-UI-SPEC.md (§Screen Layout Contract — section order; §Interaction Patterns step 5 success banner)
    - src/lib/strava/tss-chart-data.ts, src/components/strava/strava-section.tsx, src/components/strava/tss-chart.tsx (Task 1/2 outputs)
  </read_first>
  <action>
    Edit src/app/(app)/dashboard/page.tsx (RSC). Accept `searchParams` (async in Next.js 16 — `await searchParams`). After the existing profile/latestSession fetches:
      - `const connection = await findStravaConnectionByUserId(session.id)`.
      - `const allSessions = await listTrainingSessions(session.id)`.
      - `const weekly = buildWeeklyTSS(allSessions, profile?.ftp ?? null)`.
      - Convert any BigInt before passing to clients: pass StravaSection only `connection ? { athleteName: connection.athleteName } : null` (no bigint crosses the boundary, Pitfall 2). weekly buckets are plain numbers/strings — safe.
    Insert two sections in the JSX in the order from UI-SPEC §Screen Layout Contract — AFTER the "View session" link, BEFORE the "Edit profile" link:
      1. `<StravaSection connection={...} />`
      2. TSS chart Card (CardTitle=COPY.CHART_TSS_TITLE, CardDescription=COPY.CHART_TSS_DESCRIPTION, CardContent=`<TSSChart data={weekly} />`).
    Keep `space-y-6` rhythm (24px). 
    Connect-success / error banners (UI-SPEC §Interaction Patterns step 5): read `searchParams.strava_connected` / `searchParams.strava_error`. If strava_connected === "1" show an inline success message (COPY.STRAVA_CONNECTED_SUCCESS) above the Strava section. If strava_error === "scope_denied" show ErrorBanner COPY.STRAVA_ERROR_SCOPE_DENIED; for other strava_error values show a generic reconnect ErrorBanner. (No toast library — inline message only.)
    Preserve all existing dashboard content (heading, FTP status, SessionGenerator, view-session link, edit-profile link, signed-in email, logout form) unchanged.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "StravaSection\|TSSChart\|buildWeeklyTSS\|findStravaConnectionByUserId" src/app/(app)/dashboard/page.tsx` ≥ 4 (all wired)
    - `grep -c "strava_connected\|strava_error" src/app/(app)/dashboard/page.tsx` ≥ 2 (success + error banners)
    - StravaSection receives only athleteName (no bigint) — grep shows `athleteName` passed, no `stravaAthleteId` passed to a client component
    - StravaSection appears before the Edit-profile link and after the View-session link (section order preserved)
    - `npx tsc --noEmit` clean and `npx vitest run` full suite green
  </acceptance_criteria>
  <done>Dashboard shows the Strava section + TSS chart in the contracted order, surfaces connect-success and scope/error banners, and passes no BigInt across the client boundary.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The full Strava UI is wired into the dashboard: connect button, OAuth round-trip, refresh/sync, inline-confirm disconnect, and the 6-week TSS chart. This checkpoint verifies the end-to-end loop visually and functionally against a real Strava account.</what-built>
  <how-to-verify>
    1. Load /dashboard while disconnected → confirm the official "Connect with Strava" SVG button renders below the session generator (not recolored, ≥48px).
    2. Tap Connect → complete Strava OAuth (grant "View your activities") → confirm redirect back to /dashboard with the success message and the connected state ("Connected as {your name}" + Refresh + Disconnect).
    3. Confirm the TSS chart card renders at ~300px; with no matched rides it shows the empty-state label; if you have a same-day ride within ±20% duration of a generated session, tap Refresh and confirm a bar appears.
    4. Tap Disconnect → confirm the inline two-step confirm appears (no modal) → confirm → returns to disconnected state.
    5. (Optional) Decline the activity scope on connect → confirm the scope-denied ErrorBanner appears.
  </how-to-verify>
  <resume-signal>Type "approved" if the connect → sync → chart → disconnect loop works, or describe issues.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RSC → Client Component | Connection/session data crosses here; BigInt must not |
| Browser query params → dashboard | strava_connected/strava_error are reflected into the page |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Information Disclosure | athlete/token data over RSC→Client prop | mitigate | StravaSection receives only athleteName; tokens never passed; bigint String()-converted (Pitfall 2) |
| T-05-12 | Tampering | Reflected strava_error query param into ErrorBanner | mitigate | map known error codes to fixed COPY strings; never echo the raw param into the DOM |
| T-05-13 | Elevation of Privilege | Dashboard reads another user's connection | mitigate | findStravaConnectionByUserId(session.id) — userId from cookie, never client input |
| T-05-SC | Tampering | recharts install | mitigate | blocking-human legitimacy checkpoint before `npm install recharts@3.8.1`; [ASSUMED] until verified on npmjs.com |
</threat_model>

<verification>
- `npx vitest run` — full suite green (buildWeeklyTSS + all prior)
- `npx tsc --noEmit` — clean
- recharts present in package.json
- Dashboard renders Strava section + chart in contracted order; no BigInt across client boundary
- Human-verify checkpoint: connect → sync → chart → disconnect loop confirmed
</verification>

<success_criteria>
- TSS chart renders 6 orange weekly bars + empty-state overlay (PROG-02, D-13, D-15)
- buildWeeklyTSS buckets matched-session TSS correctly (PROG-02)
- Connect SVG / connected status / refresh-with-429-retry / inline-confirm disconnect all functional (STRAVA-01/02/04)
- Dashboard surfaces connect-success + scope-denied banners (D-02)
- No BigInt crosses the RSC→Client boundary (Pitfall 2)
- recharts installed via legitimacy-gated checkpoint
</success_criteria>

<output>
Create `.planning/phases/05-strava-integration/05-03-SUMMARY.md` when done
</output>
