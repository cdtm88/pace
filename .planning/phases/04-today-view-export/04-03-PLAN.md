---
phase: 04-today-view-export
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/training/zwo.ts
  - tests/zwo-export.test.ts
  - src/app/api/session/[id]/export/route.ts
autonomous: true
requirements: [TODAY-03]
must_haves:
  truths:
    - "User can download a Zwift-compatible .zwo file for their own session"
    - "warmup blocks map to <Warmup>, cooldown to <Cooldown>, work/rest to <SteadyState>"
    - "powerFraction is written as a 3-decimal fraction (e.g. 0.750)"
    - "User-supplied title and notes are XML-escaped before interpolation"
    - "Requesting another user's session id returns 404 (IDOR), not the file"
  artifacts:
    - path: "src/lib/training/zwo.ts"
      provides: "buildZwoXml + xmlEscape + sanitizeFilename pure builders"
      exports: ["buildZwoXml", "xmlEscape", "sanitizeFilename"]
    - path: "tests/zwo-export.test.ts"
      provides: "Unit coverage for XML structure, escaping, block mapping, filename sanitization"
      contains: "buildZwoXml"
    - path: "src/app/api/session/[id]/export/route.ts"
      provides: "GET export Route Handler with auth + IDOR guard + file response"
      exports: ["GET"]
  key_links:
    - from: "src/app/api/session/[id]/export/route.ts"
      to: "findTrainingSession"
      via: "IDOR-safe session fetch"
      pattern: "findTrainingSession\\(.*\\.id, id\\)"
    - from: "src/app/api/session/[id]/export/route.ts"
      to: "buildZwoXml"
      via: "import from training/zwo"
      pattern: "buildZwoXml"
---

<objective>
Deliver TODAY-03: a `GET /api/session/[id]/export` Route Handler that re-fetches the session server-side (with the IDOR `and()` guard), builds a Zwift-compatible `.zwo` XML document, and streams it as a file download. The XML builder and its helpers (XML escaping, block→element mapping, filename sanitization) live in a pure, separately tested module so the security-sensitive escaping logic has direct unit coverage.

Purpose: This is the "session → bike" half of the phase goal and is fully independent of the session-detail UI — it only reads existing queries and schema. It runs in Wave 1 in parallel with the utility/redirect foundation (Plan 01).
Output: `zwo.ts` builder module, `tests/zwo-export.test.ts`, and the export Route Handler.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-today-view-export/04-CONTEXT.md
@.planning/phases/04-today-view-export/04-RESEARCH.md
@.planning/phases/04-today-view-export/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift verification):
- `buildZwoXml(session)` — function in `src/lib/training/zwo.ts`
- `xmlEscape(str)` — function in `src/lib/training/zwo.ts`
- `sanitizeFilename(title)` — function in `src/lib/training/zwo.ts`
- `blockTypeToZwiftElement(type)` — internal helper in `src/lib/training/zwo.ts`
- `GET` — Route Handler export in `src/app/api/session/[id]/export/route.ts`
- New file path: `src/app/api/session/[id]/export/route.ts`
- `tests/zwo-export.test.ts` — new test file
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build .zwo XML module with escaping, block mapping, filename sanitization (TODAY-03)</name>
  <files>src/lib/training/zwo.ts, tests/zwo-export.test.ts</files>
  <read_first>
    - src/lib/db/schemas/session.ts (SessionBlockSchema fields: type, durationSec, powerFraction; GeneratedSession type)
    - src/lib/ai/compute-watts.ts (confirms powerFraction is ALWAYS present on every stored block — dual-path guarantee)
    - .planning/phases/04-today-view-export/04-CONTEXT.md §D-12, §D-13 (XML structure, block→element mapping, filename rules)
    - .planning/phases/04-today-view-export/04-RESEARCH.md §Pattern 4 and §Pitfall 4 and §Pitfall 6 (escape order, toFixed(3) precision)
    - tests/session-schema.test.ts (vitest style + validBlock/validSession fixtures to reuse as input shape)
  </read_first>
  <behavior>
    - xmlEscape: escapes `&` first, then `<`, `>`, `"`, `'` → `&amp; &lt; &gt; &quot; &apos;`. Input `5 × 5 < VO2 & "max"` yields the fully-escaped string with `&amp;` (no double-escaping of the ampersand).
    - blockTypeToZwiftElement: "warmup" → "Warmup"; "cooldown" → "Cooldown"; "work" → "SteadyState"; "rest" → "SteadyState".
    - buildZwoXml: output starts with `<?xml version="1.0" encoding="UTF-8"?>` and contains `<workout_file>`, `<author>Pace</author>`, `<sportType>bike</sportType>`, and a `<workout>` element.
    - buildZwoXml: a work block renders `<SteadyState Duration="{durationSec}" Power="{pf}"/>` where pf is powerFraction.toFixed(3).
    - buildZwoXml: a warmup block renders `<Warmup Duration="..." PowerLow="{pf}" PowerHigh="{pf}"/>` (same pf for both attributes).
    - buildZwoXml: powerFraction 0.75 is written as `0.750` (toFixed(3)); 0.8999999999999999 is written as `0.900`.
    - buildZwoXml: a title containing `<` or `&` appears XML-escaped inside `<name>`; notes null renders `<description></description>`.
    - sanitizeFilename: "Threshold Ladder 45 min!" → lowercased, non-[a-z0-9-_] replaced with `-`, collapsed repeats, max 50 chars (e.g. `threshold-ladder-45-min`).
  </behavior>
  <action>
    Create `src/lib/training/zwo.ts` exporting `xmlEscape(str: string): string`, `sanitizeFilename(title: string): string`, and `buildZwoXml(session): string`. Also define a non-exported `blockTypeToZwiftElement(type: string): string` helper.

    `xmlEscape` chained replaces in this exact order: `&`→`&amp;`, `<`→`&lt;`, `>`→`&gt;`, `"`→`&quot;`, `'`→`&apos;` (ampersand FIRST to avoid double-escaping — Pitfall 4).

    `blockTypeToZwiftElement`: `if (type === "warmup") return "Warmup"; if (type === "cooldown") return "Cooldown"; return "SteadyState";` (work + rest both SteadyState — D-12).

    `buildZwoXml(session)`: cast blocks via `const blocks = session.blocks as SessionBlock[]` (jsonb is typed unknown — Pitfall 2; import `GeneratedSession` and derive `type SessionBlock = GeneratedSession["blocks"][number]`). Map each block: compute `el = blockTypeToZwiftElement(b.type)` and `pf = b.powerFraction.toFixed(3)` (3 decimals — Pitfall 6). Warmup/Cooldown emit `<${el} Duration="${b.durationSec}" PowerLow="${pf}" PowerHigh="${pf}"/>`; SteadyState emits `<SteadyState Duration="${b.durationSec}" Power="${pf}"/>`. Escape `session.title` and `session.notes ?? ""` with xmlEscape BEFORE interpolating into `<name>` and `<description>`. Assemble the full document per D-12: XML declaration, `<workout_file>` with `<author>Pace</author>`, `<name>`, `<description>`, `<sportType>bike</sportType>`, and `<workout>` wrapping the joined block elements.

    `sanitizeFilename(title)`: `.toLowerCase()` → `.replace(/[^a-z0-9-_]/g, "-")` → `.replace(/-+/g, "-")` → `.slice(0, 50)`. Do NOT append `.zwo` here (the Route Handler adds the extension).

    Type the `session` parameter against the stored-row shape — accept a minimal structural type `{ title: string; notes: string | null; blocks: unknown }` so both the Drizzle `$inferSelect` row and test fixtures satisfy it.

    Create `tests/zwo-export.test.ts` covering every `<behavior>` case using vitest, importing from `@/lib/training/zwo`. Reuse a block-array fixture shaped like `tests/session-schema.test.ts` validSession.blocks.
  </action>
  <verify>
    <automated>npx vitest run tests/zwo-export.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/training/zwo.ts` contains `export function buildZwoXml(`
    - `src/lib/training/zwo.ts` contains `export function xmlEscape(`
    - `src/lib/training/zwo.ts` contains `export function sanitizeFilename(`
    - Test asserts xmlEscape escapes `&` before `<` (input `&<` → `&amp;&lt;`)
    - Test asserts a warmup block produces `PowerLow=` and `PowerHigh=` attributes
    - Test asserts a work block produces `Power=` and the value is `toFixed(3)` (e.g. `0.750`)
    - Test asserts buildZwoXml output starts with `<?xml version="1.0" encoding="UTF-8"?>`
    - Test asserts sanitizeFilename collapses repeats and caps at 50 chars
    - `npx vitest run tests/zwo-export.test.ts` exits 0
  </acceptance_criteria>
  <done>zwo.ts builds spec-compliant .zwo XML with correct escaping, block mapping, and filename sanitization; all behavior cases green.</done>
</task>

<task type="auto">
  <name>Task 2: Export Route Handler with auth + IDOR guard + file response (D-11, D-13)</name>
  <files>src/app/api/session/[id]/export/route.ts</files>
  <read_first>
    - src/app/api/auth/login/route.ts (named export, NextResponse JSON error pattern, getIronSession in a Route Handler)
    - src/lib/session.ts (sessionOptions + SessionData type)
    - src/lib/db/queries.ts (findTrainingSession signature: `(userId, sessionId)`, returns row or null)
    - src/lib/training/zwo.ts (buildZwoXml + sanitizeFilename — created in Task 1)
    - .planning/phases/04-today-view-export/04-CONTEXT.md §D-11, §D-13 (GET method, IDOR 404, response headers)
    - .planning/phases/04-today-view-export/04-RESEARCH.md §Pattern 5 and §Pitfall 1 (await params; new NextResponse(xml, { headers }))
    - .planning/phases/04-today-view-export/04-PATTERNS.md §export/route.ts (analog patterns)
  </read_first>
  <action>
    Create `src/app/api/session/[id]/export/route.ts` with a single named export `export async function GET(_req: NextRequest, { params }: { params: Promise<{ id: string }> })`.

    Body order:
    1. `const { id } = await params;` (Next.js 16 — params is async; Pitfall 1).
    2. `const ironSession = await getIronSession<SessionData>(await cookies(), sessionOptions);` — if `!ironSession.id` return `NextResponse.json({ error: "Unauthorized" }, { status: 401 })` (data endpoint returns JSON, not a redirect — matches login route pattern).
    3. `const session = await findTrainingSession(ironSession.id, id);` — if `!session` return `NextResponse.json({ error: "Not found" }, { status: 404 })` (IDOR policy: 404 never 403).
    4. Wrap XML build + response in try/catch: `const xml = buildZwoXml(session); const safeName = sanitizeFilename(session.title);` then return `new NextResponse(xml, { status: 200, headers: { "Content-Type": "application/xml", "Content-Disposition": \`attachment; filename="\${safeName}.zwo"\` } })`. On catch, log server-side and return `NextResponse.json({ error: "Export failed. Please try again." }, { status: 500 })` (matches SESSION_EXPORT_ERROR copy from Plan 01).

    Imports: `NextRequest, NextResponse` from `next/server`; `cookies` from `next/headers`; `getIronSession` from `iron-session`; `sessionOptions, type SessionData` from `@/lib/session`; `findTrainingSession` from `@/lib/db/queries`; `buildZwoXml, sanitizeFilename` from `@/lib/training/zwo`. Add a top docstring describing the security contract: userId always from iron-session (never query params — Spoofing mitigation), IDOR `and()` guard via findTrainingSession, 404 not 403.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/session/[id]/export/route.ts` contains `export async function GET(`
    - File contains `const { id } = await params`
    - File contains `findTrainingSession(ironSession.id, id)`
    - File contains `status: 404` for the no-session branch and `status: 401` for the no-auth branch
    - File contains `"Content-Disposition"` with `attachment; filename=` and `.zwo`
    - File contains `"Content-Type": "application/xml"`
    - File imports `buildZwoXml` and `sanitizeFilename` from `@/lib/training/zwo`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>GET /api/session/[id]/export auth-gates, enforces IDOR via findTrainingSession (404 on no-match), and returns the .zwo file with correct headers; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (HTTP GET) → export Route Handler | `[id]` URL segment is untrusted; userId is read server-side from iron-session, never from the URL |
| stored session text → .zwo file | title/notes are user-supplied and flow into an XML document consumed by Zwift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Elevation of Privilege | /api/session/[id]/export IDOR | mitigate | `findTrainingSession(ironSession.id, id)` uses the mandatory `and(eq(userId), eq(id))` guard; returns 404 (never 403) on no-match |
| T-04-04 | Spoofing | client-supplied userId | mitigate | userId is read exclusively from `getIronSession` server-side; never from query params or body |
| T-04-05 | Information Disclosure | unauthenticated download | mitigate | Route Handler returns 401 before any DB fetch when `!ironSession.id` |
| T-04-06 | Tampering | XML injection in .zwo | mitigate | `xmlEscape()` applied to title and notes BEFORE interpolation (ampersand-first order); unit-tested |
| T-04-SC | Tampering | npm installs | mitigate | No new packages introduced (RESEARCH §Package Legitimacy Audit: none); slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run tests/zwo-export.test.ts` — XML structure, escaping, mapping, filename green
- `npx tsc --noEmit` — Route Handler type-checks against Next.js 16 async params signature
- Manual: GET the export route for a valid owned session → 200 + `application/xml` + attachment header; GET a non-existent/other-user id → 404
</verification>

<success_criteria>
- buildZwoXml produces spec-compliant `.zwo` with correct block→element mapping and 3-decimal power fractions
- All user-supplied text is XML-escaped (ampersand-first), unit-tested
- Export Route Handler auth-gates (401), enforces IDOR (404), and returns the file with `application/xml` + attachment headers
- Type-check and zwo unit tests green
</success_criteria>

<output>
Create `.planning/phases/04-today-view-export/04-03-SUMMARY.md` when done
</output>
