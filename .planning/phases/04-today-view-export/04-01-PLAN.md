---
phase: 04-today-view-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/training/tss.ts
  - src/lib/training/format.ts
  - tests/tss.test.ts
  - src/lib/copy.ts
  - src/lib/actions/session.ts
  - src/components/session/session-generator.tsx
autonomous: true
requirements: [PROG-01, TODAY-01]
must_haves:
  truths:
    - "computeTSS returns an integer TSS for a session when FTP is set"
    - "computeTSS returns null when FTP is absent"
    - "computeIntensityLabel returns Easy/Moderate/Hard from average powerFraction when FTP is absent"
    - "After a successful generation, the user is redirected to /session/{id}"
    - "All new Phase 4 user-visible strings are sourced from COPY"
  artifacts:
    - path: "src/lib/training/tss.ts"
      provides: "computeTSS and computeIntensityLabel utilities"
      exports: ["computeTSS", "computeIntensityLabel"]
    - path: "src/lib/training/format.ts"
      provides: "Shared formatDuration utility"
      exports: ["formatDuration"]
    - path: "tests/tss.test.ts"
      provides: "Unit coverage for TSS math + intensity thresholds + null path"
      contains: "computeTSS"
    - path: "src/lib/copy.ts"
      provides: "Phase 4 session-detail copy constants"
      contains: "SESSION_PRE_RIDE_EXPORT_BTN"
  key_links:
    - from: "src/lib/actions/session.ts"
      to: "/session/{id}"
      via: "redirect() after successful DB insert"
      pattern: "redirect\\(`/session/\\$\\{inserted.id\\}`\\)"
    - from: "src/components/session/session-generator.tsx"
      to: "src/lib/training/format.ts"
      via: "import formatDuration"
      pattern: "from \"@/lib/training/format\""
---

<objective>
Build the pure-logic foundation for Phase 4: the TSS estimation utility (PROG-01), a shared duration formatter extracted from the existing dashboard component, the Phase 4 COPY constants, and the `generateSessionAction` redirect update (D-02) that sends a freshly generated session to its detail page.

Purpose: These are the dependency-free building blocks the session detail page (Plan 02) consumes. Isolating them in Wave 1 keeps the UI plan focused on rendering against a stable contract instead of inventing math and copy inline.
Output: `tss.ts`, `format.ts`, `tests/tss.test.ts`, Phase 4 keys in `copy.ts`, redirect wired into `generateSessionAction`, and `session-generator.tsx` updated to import the shared formatter.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-today-view-export/04-CONTEXT.md
@.planning/phases/04-today-view-export/04-RESEARCH.md
@.planning/phases/04-today-view-export/04-PATTERNS.md
@.planning/phases/04-today-view-export/04-UI-SPEC.md
</context>

<artifacts_this_phase_produces>
Symbols this plan creates (exclude from drift verification):
- `computeTSS(blocks, ftp)` — function in `src/lib/training/tss.ts`
- `computeIntensityLabel(blocks, ftp)` — function in `src/lib/training/tss.ts`
- `Block` — local type in `src/lib/training/tss.ts` (`{ durationSec: number; powerFraction: number }`)
- `formatDuration(totalSec)` — function in `src/lib/training/format.ts`
- COPY keys: `SESSION_PRE_RIDE_EXPORT_BTN`, `SESSION_PRE_RIDE_START_BTN`, `SESSION_COMPLETE_HEADING`, `SESSION_COMPLETE_BACK`, `SESSION_BADGE_TSS`, `SESSION_BADGE_INTENSITY`, `SESSION_DASHBOARD_VIEW_LINK`, `SESSION_BLOCK_COUNTER`, `SESSION_EXPORT_ERROR`
- `tests/tss.test.ts` — new test file
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TSS + intensity utility with unit tests (PROG-01)</name>
  <files>src/lib/training/tss.ts, tests/tss.test.ts</files>
  <read_first>
    - src/lib/training/zones.ts (module header style, pure-function + null-when-no-FTP convention to mirror exactly)
    - tests/zones.test.ts (vitest describe/it/expect style and import alias `@/lib/training/...`)
    - src/lib/db/schemas/session.ts (SessionBlockSchema — confirms durationSec and powerFraction are present on every stored block)
    - .planning/phases/04-today-view-export/04-CONTEXT.md §D-10 (exact formula and thresholds)
  </read_first>
  <behavior>
    - computeTSS: returns null when ftp is null/undefined/0.
    - computeTSS: returns null when totalSec across blocks is 0.
    - computeTSS: for blocks totaling 3600s at a uniform powerFraction of 1.0, returns 100 (TSS = 3600 × 1.0² × 100 / 3600).
    - computeTSS: weights IF by durationSec — a 600s block at 0.5 plus a 600s block at 1.0 yields weightedIF = 0.75; result = Math.round(1200 × 0.75² × 100 / 3600) = 19.
    - computeTSS: result is always an integer (Math.round applied).
    - computeIntensityLabel: returns null when ftp is set (TSS shown instead).
    - computeIntensityLabel: returns "Easy" when avg powerFraction < 0.65.
    - computeIntensityLabel: returns "Moderate" when avg powerFraction is in [0.65, 0.80).
    - computeIntensityLabel: returns "Hard" when avg powerFraction >= 0.80.
    - computeIntensityLabel: returns null when totalSec is 0.
  </behavior>
  <action>
    Create `src/lib/training/tss.ts` exporting `computeTSS(blocks, ftp)` and `computeIntensityLabel(blocks, ftp)`. Define a local `type Block = { durationSec: number; powerFraction: number }`. Both functions take `(blocks: Block[], ftp: number | null)`.

    `computeTSS` signature returns `number | null`: guard `if (!ftp) return null;`, compute `totalSec` via reduce, return null if `totalSec === 0`, compute `weightedIF = sum(powerFraction × durationSec) / totalSec`, return `Math.round((totalSec * weightedIF * weightedIF * 100) / 3600)`. Use the squared weightedIF directly (`weightedIF * weightedIF`), matching D-10's `TSS = durationSec × IF² × 100 / 3600`.

    `computeIntensityLabel` signature returns `"Easy" | "Moderate" | "Hard" | null`: guard `if (ftp) return null;`, compute the same `totalSec` and weighted `avgFraction`, return null if `totalSec === 0`, then map `< 0.65 → "Easy"`, `< 0.80 → "Moderate"`, else `"Hard"` (thresholds from D-10).

    Mirror the zones.ts module-header docstring style: state that these are pure TS with zero deps, that computeTSS returns null on the no-FTP path (PROG-01), and list "Used by: Phase 4 session page (server-side, passed as prop)".

    Create `tests/tss.test.ts` covering every case in `<behavior>` using vitest `describe`/`it`/`expect`, importing from `@/lib/training/tss`. Mirror the style of `tests/zones.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/tss.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/training/tss.ts` contains `export function computeTSS(`
    - `src/lib/training/tss.ts` contains `export function computeIntensityLabel(`
    - `npx vitest run tests/tss.test.ts` exits 0
    - Test for 3600s @ 1.0 powerFraction asserts `computeTSS(...) === 100`
    - Test asserts `computeTSS(blocks, null) === null`
    - Test asserts `computeIntensityLabel(blocks, someFtp) === null` and intensity threshold mappings for Easy/Moderate/Hard
  </acceptance_criteria>
  <done>computeTSS and computeIntensityLabel implemented per D-10 with all behavior cases green in tss.test.ts.</done>
</task>

<task type="auto">
  <name>Task 2: Extract shared formatDuration utility</name>
  <files>src/lib/training/format.ts, src/components/session/session-generator.tsx</files>
  <read_first>
    - src/components/session/session-generator.tsx (lines 58–67 hold the local formatDuration to extract; lines 27–33 show import style)
    - src/lib/training/zones.ts (module-header docstring style to mirror)
  </read_first>
  <action>
    Create `src/lib/training/format.ts` exporting `formatDuration(totalSec: number): string`. Move the exact implementation currently inline in `session-generator.tsx` (lines 58–67): `Math.round(totalSec / 60)`; `< 60` returns `"${totalMin} min"`; otherwise `hours = Math.floor(totalMin / 60)`, `mins = totalMin % 60`, return `"${hours}h ${mins}m"` when `mins > 0` else `"${hours}h"`. Add a one-line docstring matching the existing comment "Format total duration in seconds to human-readable 'X min' or 'Xh Ym'."

    In `session-generator.tsx`: delete the local `formatDuration` function definition and add `import { formatDuration } from "@/lib/training/format";` alongside the other `@/lib/training` / `@/lib` imports. Do not change any other behavior in the component — the existing `formatDuration(sessionData.totalDurationSec)` call site stays identical.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/training/format.ts` contains `export function formatDuration(`
    - `src/components/session/session-generator.tsx` contains `from "@/lib/training/format"`
    - `src/components/session/session-generator.tsx` does NOT contain a local `function formatDuration(` definition (grep: `grep -v '^ \*' src/components/session/session-generator.tsx | grep -c 'function formatDuration'` returns 0)
    - `npx tsc --noEmit` exits 0
    - Full vitest suite exits 0 (no regressions)
  </acceptance_criteria>
  <done>formatDuration lives in src/lib/training/format.ts; session-generator.tsx imports it; no duplicate definition remains; type-check and full suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 4 COPY constants and wire generateSessionAction redirect (D-02)</name>
  <files>src/lib/copy.ts, src/lib/actions/session.ts</files>
  <read_first>
    - src/lib/copy.ts (existing `COPY` object, section-header comment style, DASHBOARD_FTP_ACTIVE substitution-token pattern at line 69)
    - src/lib/actions/session.ts (current return-only flow; lines 161–188 show DB insert try/catch then `return { data: inserted }`)
    - .planning/phases/04-today-view-export/04-UI-SPEC.md §Copywriting Contract (exact copy strings, `·` U+00B7 separator)
    - .planning/phases/04-today-view-export/04-RESEARCH.md §Pitfall 3 and §Pitfall 7 (redirect must be OUTSIDE try/catch; useTransition interaction)
  </read_first>
  <action>
    In `src/lib/copy.ts`, add a new section comment `// ── Phase 4: Session detail ──` after the Phase 2 sections and before the closing `} as const;`. Add these exact keys/values (use `·` U+00B7 middle dot, not `•`):
    - `SESSION_PRE_RIDE_EXPORT_BTN: "Export .zwo"`
    - `SESSION_PRE_RIDE_START_BTN: "Start session"`
    - `SESSION_COMPLETE_HEADING: "Session complete"`
    - `SESSION_COMPLETE_BACK: "Back to dashboard"`
    - `SESSION_BADGE_TSS: "~{tss} TSS · {duration}"`
    - `SESSION_BADGE_INTENSITY: "{intensity} · {duration}"`
    - `SESSION_DASHBOARD_VIEW_LINK: "View session"`
    - `SESSION_BLOCK_COUNTER: "{current} / {total}"`
    - `SESSION_EXPORT_ERROR: "Export failed. Please try again."`

    In `src/lib/actions/session.ts` (D-02): add `import { redirect } from "next/navigation";` to the import block. Replace the final `return { data: inserted }` (line 187) with `redirect(\`/session/\${inserted.id}\`)`. The `redirect()` call MUST be the last statement, placed AFTER the `if (!inserted) { return ... }` guard and OUTSIDE the DB-insert try/catch (Pitfall 3 — redirect throws NEXT_REDIRECT; catching it suppresses navigation). Keep all error-path returns (`return { error: ... }`) unchanged. Update the function return type annotation: change `Promise<{ data?: ...; error?: string }>` so the success path no longer returns data — set it to `Promise<{ error?: string } | never>` (or `Promise<{ error: string } | undefined>`), since success now navigates instead of returning. Update the Step 8 docstring comment block (lines ~22–23 and the "8. Return" line) to state "8. Success: redirect to /session/{id} (D-02); error paths return { error }".

    Do NOT modify `session-generator.tsx` result-rendering branches in this task — Plan 02 handles any cleanup of the now-unreachable success card. The component still compiles because `setResult(res as ActionResult)` accepts the error-or-undefined shape.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/copy.ts` contains `SESSION_PRE_RIDE_EXPORT_BTN: "Export .zwo"`
    - `src/lib/copy.ts` contains `SESSION_BADGE_TSS: "~{tss} TSS · {duration}"` (with U+00B7 middle dot)
    - `src/lib/copy.ts` contains `SESSION_BLOCK_COUNTER: "{current} / {total}"`
    - `src/lib/actions/session.ts` contains `import { redirect } from "next/navigation"`
    - `src/lib/actions/session.ts` contains ``redirect(`/session/${inserted.id}`)``
    - `src/lib/actions/session.ts` does NOT contain `return { data: inserted }`
    - The `redirect(...)` call appears after the `if (!inserted)` guard and is not inside a try block
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Phase 4 COPY keys present with exact strings; generateSessionAction redirects to /session/{id} on success with redirect placed outside try/catch; type-check passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (useTransition call) → generateSessionAction | readinessScore crosses from browser; userId is read server-side from iron-session, never from client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | generateSessionAction redirect target | accept | redirect URL uses `inserted.id` from the server-side DB insert (owned by the authenticated user); no client-supplied id reaches the redirect |
| T-04-02 | Tampering | redirect() placement | mitigate | redirect() placed OUTSIDE try/catch so a swallowed NEXT_REDIRECT cannot silently drop the user on the dashboard (Pitfall 3) |
| T-04-SC | Tampering | npm installs | mitigate | No new packages introduced this plan (RESEARCH §Package Legitimacy Audit: none); slopcheck N/A |
</threat_model>

<verification>
- `npx vitest run tests/tss.test.ts` — TSS + intensity math green
- `npx tsc --noEmit` — no type regressions across the redirect + copy changes
- `npx vitest run` — full suite green (formatDuration extraction introduces no regression)
- Manual grep: `redirect(` appears in session.ts outside any try block
</verification>

<success_criteria>
- computeTSS / computeIntensityLabel implemented per D-10 with full unit coverage
- formatDuration extracted and reused (no duplicate definition)
- All 9 Phase 4 COPY keys present with exact strings and U+00B7 separator
- generateSessionAction redirects to `/session/{id}` on success; error returns preserved
- Type-check and full test suite green
</success_criteria>

<output>
Create `.planning/phases/04-today-view-export/04-01-SUMMARY.md` when done
</output>
