---
phase: 02-profile-onboarding
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/lib/actions/profile.ts
  - src/lib/copy.ts
  - src/components/ui/textarea.tsx
  - src/components/ui/progress.tsx
  - src/components/onboarding/onboarding-wizard.tsx
  - src/app/(onboarding)/onboarding/page.tsx
  - src/app/(app)/layout.tsx
  - proxy.ts
autonomous: false
requirements: [PROF-01]
user_setup: []
must_haves:
  truths:
    - "New user (onboardingComplete=false) hitting any (app) route is redirected to /onboarding"
    - "User completes the 3-step wizard and the profile is saved with onboardingComplete=true"
    - "On wizard submit the app revalidates /dashboard then redirects to /dashboard"
    - "Onboarding route does not loop — it lives in (onboarding) group, gated on session only"
  artifacts:
    - path: "src/lib/actions/profile.ts"
      provides: "saveProfileAction Server Action (auth + Zod + upsert + revalidate + redirect)"
      exports: ["saveProfileAction"]
    - path: "src/components/onboarding/onboarding-wizard.tsx"
      provides: "3-step client wizard"
      contains: "use client"
    - path: "src/app/(onboarding)/onboarding/page.tsx"
      provides: "Onboarding RSC shell (session-only gate)"
      contains: "OnboardingWizard"
    - path: "src/app/(app)/layout.tsx"
      provides: "Onboarding redirect gate"
      contains: "findUserProfileByUserId"
    - path: "src/components/ui/textarea.tsx"
      provides: "shadcn Textarea"
      contains: "Textarea"
    - path: "src/components/ui/progress.tsx"
      provides: "shadcn Progress"
      contains: "Progress"
  key_links:
    - from: "src/components/onboarding/onboarding-wizard.tsx"
      to: "src/lib/actions/profile.ts"
      via: "useActionState(saveProfileAction, null) + form action"
      pattern: "saveProfileAction"
    - from: "src/app/(app)/layout.tsx"
      to: "src/lib/db/queries.ts"
      via: "findUserProfileByUserId(session.id) onboarding gate"
      pattern: "findUserProfileByUserId"
    - from: "src/lib/actions/profile.ts"
      to: "userProfiles table"
      via: "db.insert().onConflictDoUpdate({ target: userProfiles.userId })"
      pattern: "onConflictDoUpdate"
---

<objective>
Build the onboarding flow: the `saveProfileAction` Server Action (auth → Zod parse → upsert → revalidate → redirect), extend copy.ts with Phase 2 strings, install the shadcn Textarea + Progress components, build the 3-step client wizard, the `(onboarding)` route-group RSC shell, the `(app)` layout onboarding gate, and confirm `/onboarding` routing in proxy.ts.

Purpose: Delivers PROF-01 end-to-end — a new user is guided through the wizard and lands on the dashboard with a complete profile. The Server Action is the single mutation path reused by Plan 03's profile edit page.
Output: Server Action, extended copy, two shadcn components, wizard component, onboarding page, modified layout + proxy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profile-onboarding/02-RESEARCH.md
@.planning/phases/02-profile-onboarding/02-PATTERNS.md
@.planning/phases/02-profile-onboarding/02-UI-SPEC.md
@.planning/phases/02-profile-onboarding/02-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Install shadcn Textarea + Progress (package-source verify)</name>
  <files>src/components/ui/textarea.tsx, src/components/ui/progress.tsx, package.json</files>
  <action>
    Run `npx shadcn@latest add textarea` then `npx shadcn@latest add progress` from the project root. components.json declares `registries: {}` (official shadcn registry only, base-nova style) — do not add any third-party registry. These commands write `src/components/ui/textarea.tsx` and `src/components/ui/progress.tsx` and may add or version-bump a `@base-ui-components/react` dependency in package.json. After running, STOP and present the human-verify steps below before building or wiring anything.
  </action>
  <verify>
    <automated>test -f src/components/ui/textarea.tsx && test -f src/components/ui/progress.tsx</automated>
  </verify>
  <what-built>
    Two shadcn components are added via the official registry: `npx shadcn@latest add textarea` and `npx shadcn@latest add progress`. These write `src/components/ui/textarea.tsx` and `src/components/ui/progress.tsx`. The shadcn `add` command pulls @base-ui/react primitives (base-nova style, per components.json) — Progress wraps the Base UI Progress primitive. This is the only dependency-adding step in Phase 2.
  </what-built>
  <how-to-verify>
    1. Confirm the registry is shadcn official only (components.json `registries: {}` — no third-party). The RESEARCH Package Legitimacy Audit lists zero new npm packages; shadcn components are source files, not packages, but any transitive @base-ui/react install must be verified.
    2. After running both add commands, check `git diff package.json` — if `@base-ui-components/react` (or a `@base-ui/*` package) is newly added or version-bumped, verify it on https://www.npmjs.com/package/@base-ui-components/react (the published Base UI package) for legitimacy: maintained, reasonable download count, matches the version shadcn base-nova expects.
    3. Confirm `src/components/ui/textarea.tsx` and `src/components/ui/progress.tsx` now exist and export `Textarea` / `Progress`.
  </how-to-verify>
  <resume-signal>Type "approved" once both component files exist and any new @base-ui dependency is confirmed legitimate, or describe what looks wrong.</resume-signal>
  <done>Both component files exist and export Textarea / Progress; any new @base-ui dependency is human-verified as legitimate on npmjs.com.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: saveProfileAction Server Action + copy.ts strings</name>
  <files>src/lib/actions/profile.ts, src/lib/copy.ts</files>
  <read_first>
    - src/app/api/auth/login/route.ts (session auth pattern: getIronSession(await cookies(), sessionOptions); Zod safeParse + issue return)
    - src/lib/session.ts (SessionData has id + email; sessionOptions export)
    - src/lib/db/schemas/profile.ts (profileSchema from Plan 01 — the parse target)
    - src/lib/db/schema.ts (userProfiles with .unique() userId + onboardingComplete from Plan 01)
    - src/lib/db/index.ts (the `db` export to import)
    - src/lib/copy.ts (existing COPY object — append Phase 2 keys inside the same `as const` object)
    - .planning/phases/02-profile-onboarding/02-PATTERNS.md (actions/profile.ts full Server Action structure)
    - .planning/phases/02-profile-onboarding/02-RESEARCH.md (Pattern 2 upsert; Pitfall 2 empty-string→undefined; Pitfall 5 revalidate-before-redirect)
    - .planning/phases/02-profile-onboarding/02-UI-SPEC.md (Copywriting Contract table — exact strings + keys)
  </read_first>
  <action>
    Create src/lib/actions/profile.ts with `'use server'` at the top. Import `cookies` from `next/headers`, `getIronSession` from `iron-session`, `sessionOptions` + `SessionData` from `@/lib/session`, `db` from `@/lib/db/index`, `userProfiles` from `@/lib/db/schema`, `revalidatePath` from `next/cache`, `redirect` from `next/navigation`, `profileSchema` from `@/lib/db/schemas/profile`. Export `async function saveProfileAction(_prevState: unknown, formData: FormData)`. Body in this exact order: (1) `const session = await getIronSession<SessionData>(await cookies(), sessionOptions)`; if `!session.id` call `redirect('/login')`. (2) `profileSchema.safeParse({ goals: formData.get('goals'), injuries: formData.get('injuries'), ftp: formData.get('ftp') || undefined, weight: formData.get('weight') || undefined })` — the `|| undefined` on ftp/weight is mandatory (Pitfall 2: empty FormData string coerces to NaN otherwise). (3) On `!result.success` return `{ errors: result.error.issues }` (`.issues`, not `.errors` — Zod v4). (4) `await db.insert(userProfiles).values({ userId: session.id, ...result.data, onboardingComplete: true, updatedAt: new Date() }).onConflictDoUpdate({ target: userProfiles.userId, set: { ...result.data, onboardingComplete: true, updatedAt: new Date() } })`. (5) `revalidatePath('/dashboard')` BEFORE `redirect('/dashboard')` (Pitfall 5: redirect throws). Do NOT call `session.save()` — profile mutations don't touch the session (Phase 1 architecture rule).

    In src/lib/copy.ts: append the Phase 2 keys from the UI-SPEC Copywriting Contract inside the existing `COPY` object (before the closing `} as const`). Use these exact key→value pairs verbatim from the UI-SPEC table: ONBOARDING_STEP1_HEADING, ONBOARDING_STEP1_HELPER, ONBOARDING_STEP2_HEADING, ONBOARDING_STEP2_HELPER, ONBOARDING_STEP3_HEADING, ONBOARDING_STEP3_HELPER, ONBOARDING_CTA_NEXT ("Next"), ONBOARDING_CTA_BACK ("Back"), ONBOARDING_CTA_SUBMIT ("Get started"), ONBOARDING_CTA_LOADING ("Setting up your profile…"), PROFILE_EDIT_HEADING, PROFILE_EDIT_SUBTEXT, PROFILE_CTA_SAVE, PROFILE_CTA_LOADING, DASHBOARD_WELCOME, DASHBOARD_EMPTY_HEADING, DASHBOARD_EMPTY_BODY, DASHBOARD_FTP_ACTIVE, DASHBOARD_FTP_ABSENT, DASHBOARD_LINK_EDIT_PROFILE, FIELD_ERROR_GOALS_REQUIRED ("Describe your training goals to continue."), FIELD_ERROR_FTP_RANGE, FIELD_ERROR_WEIGHT_RANGE, FIELD_LABEL_GOALS, FIELD_LABEL_INJURIES, FIELD_LABEL_FTP, FIELD_LABEL_WEIGHT, FIELD_PLACEHOLDER_GOALS, FIELD_PLACEHOLDER_INJURIES, FIELD_PLACEHOLDER_FTP, FIELD_PLACEHOLDER_WEIGHT, FIELD_UNIT_FTP ("watts"), FIELD_UNIT_WEIGHT ("kg"), FIELD_OPTIONAL_LABEL ("Optional"), STEP_COUNTER ("Step {n} of 3"). For DASHBOARD_FTP_ACTIVE keep the `{value}` placeholder token in the string (the dashboard substitutes it in Plan 03).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/actions/profile.ts` starts with `'use server'` and exports `saveProfileAction`
    - The action reads userId from session only — no `formData.get('userId')` anywhere (grep: `grep -c "formData.get('userId')" src/lib/actions/profile.ts` returns 0)
    - `revalidatePath('/dashboard')` appears textually BEFORE `redirect('/dashboard')` in the file
    - The upsert uses `onConflictDoUpdate` with `target: userProfiles.userId`
    - ftp/weight passed as `formData.get('ftp') || undefined` / `formData.get('weight') || undefined`
    - `src/lib/copy.ts` contains keys `ONBOARDING_STEP1_HEADING`, `ONBOARDING_CTA_SUBMIT`, `DASHBOARD_FTP_ABSENT`, `STEP_COUNTER` (grep each returns ≥1)
    - `npx tsc --noEmit` passes; `npx vitest run` stays green
  </acceptance_criteria>
  <done>saveProfileAction implements auth→Zod→upsert→revalidate→redirect; copy.ts carries all Phase 2 strings; typecheck + suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Onboarding wizard + (onboarding) page + (app) gate + proxy</name>
  <files>src/components/onboarding/onboarding-wizard.tsx, src/app/(onboarding)/onboarding/page.tsx, src/app/(app)/layout.tsx, proxy.ts</files>
  <read_first>
    - src/components/auth/login-form.tsx ('use client' structure, field-with-error pattern, Loader2 button, h-12 text-base inputs)
    - src/components/auth/auth-card.tsx (centered card layout, wordmark, max-w-sm — wizard uses max-w-lg)
    - src/components/ui/textarea.tsx + src/components/ui/progress.tsx (installed in Task 1 — confirm export names)
    - src/components/ui/error-banner.tsx (ErrorBanner message prop, role="alert")
    - src/lib/copy.ts (Phase 2 COPY keys from Task 2)
    - src/lib/actions/profile.ts (saveProfileAction from Task 2 — useActionState target)
    - src/app/(app)/layout.tsx (current session-only check lines 19-35 — extend with onboarding gate)
    - src/app/(auth)/login/page.tsx (RSC shell analog for the onboarding page)
    - src/lib/db/queries.ts (findUserProfileByUserId from Plan 01)
    - proxy.ts (PUBLIC_PATHS list lines 29-35, session gate lines 49-59 — confirm /onboarding handling)
    - .planning/phases/02-profile-onboarding/02-UI-SPEC.md (Screen 1 wizard contract; Interaction Contracts; iOS auto-zoom text-base requirement)
    - .planning/phases/02-profile-onboarding/02-RESEARCH.md (Pattern 6 gate + Pitfall 1 loop; Pattern 4 useActionState)
    - .planning/phases/02-profile-onboarding/02-PATTERNS.md (onboarding-wizard.tsx section — useState step state, back button type="button", card layout max-w-lg)
  </read_first>
  <action>
    Create src/components/onboarding/onboarding-wizard.tsx as `'use client'`. State: `const [data, setData] = useState({ step: 1, goals: "", injuries: "", ftp: "", weight: "" })` plus `const [goalsError, setGoalsError] = useState<string | null>(null)`. Use `useActionState(saveProfileAction, null)` from `react` for the final-step form; `useFormStatus` from `react-dom` only inside a child submit-button component (it cannot read pending in the same component that renders the form). Layout: full-viewport `flex min-h-screen flex-col items-center justify-center bg-background px-6 py-12`, wordmark (COPY.WORDMARK, 28px/700), a `Progress` bar (`h-2 w-full max-w-lg`, value = step/3*100), a right-aligned step counter using COPY.STEP_COUNTER with `{n}` replaced by the current step, then a `Card`/`CardContent p-8` with `max-w-lg`.

    Steps render conditionally on `data.step`: Step 1 = Goals `Textarea` (label COPY.FIELD_LABEL_GOALS, placeholder COPY.FIELD_PLACEHOLDER_GOALS, `min-h-[120px] text-base`, required); heading COPY.ONBOARDING_STEP1_HEADING (`<h1 className="text-2xl font-semibold leading-[1.2] text-foreground">`), helper COPY.ONBOARDING_STEP1_HELPER; only a "Next" button. Step 2 = Injuries `Textarea` (label with COPY.FIELD_OPTIONAL_LABEL span in `text-sm font-normal text-muted-foreground`, placeholder COPY.FIELD_PLACEHOLDER_INJURIES, `min-h-[100px] text-base`, not required); "Back" (ghost, type="button") + "Next". Step 3 = FTP `Input` (`type="number" inputMode="numeric"`, label + Optional, placeholder COPY.FIELD_PLACEHOLDER_FTP, unit COPY.FIELD_UNIT_FTP, `h-12 text-base`) and Weight `Input` (`type="number" inputMode="decimal"`, placeholder COPY.FIELD_PLACEHOLDER_WEIGHT, unit COPY.FIELD_UNIT_WEIGHT, `h-12 text-base`); wrapped in `<form action={formAction}>` with named inputs carrying `name="goals"|"injuries"|"ftp"|"weight"` so the Server Action FormData receives all four fields (carry goals/injuries from earlier-step state via hidden inputs with the stored values; ftp/weight are visible inputs on step 3). "Back" (ghost) + submit button "Get started" showing Loader2 + COPY.ONBOARDING_CTA_LOADING when pending (via the child SubmitButton using useFormStatus). Render `{state?.errors && <ErrorBanner message={COPY.AUTH_ERROR_SERVER} />}` above the submit row.

    Step navigation: "Next" on Step 1 is `type="button"` — onClick validates `data.goals.trim()` non-empty; if empty set goalsError to COPY.FIELD_ERROR_GOALS_REQUIRED and set `aria-invalid` + `aria-describedby` on the textarea; if valid `setData(d => ({...d, step: 2}))`. "Next" on Step 2 advances to step 3 with no validation. "Back" buttons decrement step and preserve all field values (they live in `data` state). All Back/Next buttons are `type="button"` to avoid submitting the form prematurely; only "Get started" is `type="submit"`.

    Create src/app/(onboarding)/onboarding/page.tsx as an async RSC: `getIronSession<SessionData>(await cookies(), sessionOptions)`; if `!session.id` redirect('/login'). Do NOT check onboardingComplete here (this IS the onboarding page — checking it would loop). Render `<OnboardingWizard />`.

    Modify src/app/(app)/layout.tsx: after the existing `if (!session.id) redirect("/login")` block, add `import { findUserProfileByUserId } from "@/lib/db/queries"` and `const profile = await findUserProfileByUserId(session.id); if (!profile || !profile.onboardingComplete) { redirect("/onboarding"); }`. This gate must live ONLY in the (app) layout, never in (onboarding) — Pitfall 1 loop prevention.

    proxy.ts: do NOT add `/onboarding` to PUBLIC_PATHS — `/onboarding` must remain session-protected (a logged-out user hitting it should be sent to /login). proxy.ts already passes any path with a valid `session.id` through `NextResponse.next()`, so an authenticated user reaches /onboarding fine; the (onboarding) route group has no onboardingComplete gate, so no loop occurs. Add a one-line comment above the matcher noting that `/onboarding` is intentionally session-protected (not in PUBLIC_PATHS) and that its onboardingComplete gate is deliberately absent to prevent the redirect loop (Pitfall 1). If during execution an authenticated user is wrongly redirected from /onboarding to /login, the cause is a session-cookie read issue — debug that; do NOT add /onboarding to PUBLIC_PATHS.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/onboarding/onboarding-wizard.tsx` starts with `"use client"` and calls `useActionState` imported from `react` (grep: `grep -c "from \"react\"" src/components/onboarding/onboarding-wizard.tsx` ≥1) — NOT `useFormState`
    - Wizard has named inputs `name="goals"`, `name="injuries"`, `name="ftp"`, `name="weight"` so the Server Action FormData is populated (grep each returns ≥1)
    - Back/Next buttons carry `type="button"`; only the final submit is `type="submit"`
    - `src/app/(onboarding)/onboarding/page.tsx` checks `session.id` but does NOT reference `onboardingComplete` (grep: `grep -c onboardingComplete "src/app/(onboarding)/onboarding/page.tsx"` returns 0)
    - `src/app/(app)/layout.tsx` calls `findUserProfileByUserId` and redirects to `/onboarding` when `!profile || !profile.onboardingComplete`
    - `/onboarding` is NOT present in proxy.ts PUBLIC_PATHS (grep: `grep -c '"/onboarding"' proxy.ts` returns 0)
    - All inputs/textareas carry `text-base` (iOS auto-zoom prevention, PWA-05 prerequisite)
    - `npx tsc --noEmit` passes, `npx vitest run` green, `npx next build` completes without route/build errors
  </acceptance_criteria>
  <done>3-step wizard wired to saveProfileAction; onboarding page gated on session only; (app) layout redirects unboarded users; build green with no redirect loop.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 02)

New files:
- `src/lib/actions/profile.ts` — exports `saveProfileAction(_prevState, formData)` ('use server')
- `src/components/ui/textarea.tsx` — exports `Textarea` (shadcn)
- `src/components/ui/progress.tsx` — exports `Progress` (shadcn)
- `src/components/onboarding/onboarding-wizard.tsx` — exports `OnboardingWizard` ('use client')
- `src/app/(onboarding)/onboarding/page.tsx` — default export `OnboardingPage` (RSC)

Modified files / new symbols:
- `src/lib/copy.ts` — COPY object gains all Phase 2 keys (ONBOARDING_*, PROFILE_*, DASHBOARD_*, FIELD_*, STEP_COUNTER)
- `src/app/(app)/layout.tsx` — onboarding redirect gate via findUserProfileByUserId
- `proxy.ts` — comment clarifying /onboarding session protection (PUBLIC_PATHS unchanged)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Server Action | Untrusted FormData crosses into saveProfileAction |
| browser → (app) layout | Unboarded/unauthenticated requests gated server-side |
| shadcn registry → repo | Component source + transitive @base-ui dependency |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (IDOR) | saveProfileAction | mitigate | userId read from iron-session cookie only; form never includes a userId field; client-submitted userId is impossible to honor. |
| T-02-04 | Elevation of Privilege | saveProfileAction | mitigate | `if (!session.id) redirect('/login')` at the top before any DB write. |
| T-02-05 | Denial of Service | goals/injuries text fields | mitigate | profileSchema `.max(1000)` (Plan 01) enforced at parse; oversized payloads rejected before insert. |
| T-02-06 | Broken Access Control | onboarding redirect gate | mitigate | (app) layout redirects `!onboardingComplete` users to /onboarding; (onboarding) group has NO onboardingComplete gate (Pitfall 1) — no loop, no bypass. /onboarding stays out of PUBLIC_PATHS so logged-out users are still sent to /login. |
| T-02-SC | Tampering | shadcn add (textarea, progress) | mitigate | Task 1 blocking-human checkpoint verifies the official shadcn registry and any new @base-ui dependency on npmjs.com before build. |
</threat_model>

<verification>
- `npx next build` completes — confirms no infinite redirect at route-group boundary and no RSC/client boundary errors
- `npx vitest run` full suite green (Plan 01 tests unaffected)
- Wizard submit reaches saveProfileAction; on success redirects to /dashboard (verified at runtime in Plan 03's human-verify, since dashboard wiring lands there)
- No `formData.get('userId')` anywhere; no `useFormState` import; `/onboarding` not in PUBLIC_PATHS
</verification>

<success_criteria>
- saveProfileAction performs auth → Zod parse → upsert (onConflictDoUpdate) → revalidatePath → redirect
- 3-step wizard collects goals (required), injuries/ftp/weight (optional) and submits all four via FormData
- New unboarded user is redirected from (app) routes to /onboarding without looping
- copy.ts holds every Phase 2 UI string
- Typecheck + full test suite + production build all green
</success_criteria>

<output>
Create `.planning/phases/02-profile-onboarding/02-02-SUMMARY.md` when done
</output>
