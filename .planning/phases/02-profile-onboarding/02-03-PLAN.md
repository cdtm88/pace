---
phase: 02-profile-onboarding
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/profile/profile-form.tsx
  - src/app/(app)/profile/page.tsx
  - src/app/(app)/dashboard/page.tsx
autonomous: false
requirements: [PROF-02, PROF-03]
user_setup: []
must_haves:
  truths:
    - "User can open /profile, see existing values pre-populated, edit any field, and save"
    - "Saving an updated profile updates the existing row (no duplicate) and returns to /dashboard"
    - "User can add FTP later via /profile after onboarding without it"
    - "Dashboard shows 'FTP: {value}W · Coggan zones active' when FTP set, 'No FTP set · Using RPE mode' when absent"
  artifacts:
    - path: "src/components/profile/profile-form.tsx"
      provides: "Pre-populated profile edit form using saveProfileAction"
      contains: "useActionState"
    - path: "src/app/(app)/profile/page.tsx"
      provides: "Profile edit RSC page (fetches existing profile)"
      contains: "findUserProfileByUserId"
    - path: "src/app/(app)/dashboard/page.tsx"
      provides: "FTP-status + edit-profile-link dashboard"
      contains: "DASHBOARD_FTP"
  key_links:
    - from: "src/app/(app)/profile/page.tsx"
      to: "src/lib/db/queries.ts"
      via: "findUserProfileByUserId(session.id) pre-population"
      pattern: "findUserProfileByUserId"
    - from: "src/components/profile/profile-form.tsx"
      to: "src/lib/actions/profile.ts"
      via: "useActionState(saveProfileAction, null)"
      pattern: "saveProfileAction"
    - from: "src/app/(app)/dashboard/page.tsx"
      to: "src/lib/training/zones.ts | profile.ftp"
      via: "FTP presence check drives zone-active vs RPE copy"
      pattern: "DASHBOARD_FTP"
---

<objective>
Build the profile editing surface and dashboard wiring: a pre-populated `ProfileForm` client component using `useActionState(saveProfileAction)`, the `/profile` RSC page that fetches the existing profile, and the dashboard update that fetches the profile and renders FTP-active vs RPE-mode status plus an "Edit profile" link. A final human-verify checkpoint walks the full onboarding → dashboard → edit-FTP → dashboard loop in the browser.

Purpose: Delivers PROF-02 (edit anytime, add FTP later) and the FTP-present-vs-absent display half of PROF-03. Reuses the Plan 02 Server Action — no new mutation logic.
Output: Profile form component, /profile page, modified dashboard, browser verification.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profile-onboarding/02-RESEARCH.md
@.planning/phases/02-profile-onboarding/02-PATTERNS.md
@.planning/phases/02-profile-onboarding/02-UI-SPEC.md
@.planning/phases/02-profile-onboarding/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: ProfileForm component + /profile RSC page</name>
  <files>src/components/profile/profile-form.tsx, src/app/(app)/profile/page.tsx</files>
  <read_first>
    - src/components/auth/login-form.tsx (client field patterns, h-12 text-base, Loader2 button, error display)
    - src/components/onboarding/onboarding-wizard.tsx (Plan 02 — Textarea/Input usage, COPY keys, useActionState + child SubmitButton via useFormStatus)
    - src/lib/actions/profile.ts (Plan 02 saveProfileAction — same action reused; it redirects to /dashboard on success)
    - src/lib/db/schemas/profile.ts (ProfileInput type for the `existing` prop)
    - src/lib/db/queries.ts (findUserProfileByUserId — pre-population source)
    - src/app/(app)/dashboard/page.tsx (RSC session pattern to mirror for /profile)
    - src/components/ui/card.tsx (Card/CardContent API)
    - src/lib/copy.ts (PROFILE_EDIT_HEADING, PROFILE_EDIT_SUBTEXT, PROFILE_CTA_SAVE, PROFILE_CTA_LOADING, FIELD_* keys)
    - .planning/phases/02-profile-onboarding/02-UI-SPEC.md (Screen 3 Profile Edit contract; pre-population via defaultValue)
    - .planning/phases/02-profile-onboarding/02-PATTERNS.md (profile-form.tsx section — SubmitButton + useFormStatus child constraint)
  </read_first>
  <action>
    Create src/components/profile/profile-form.tsx as `'use client'`. Import `useActionState` from `react`, `useFormStatus` from `react-dom`, `saveProfileAction` from `@/lib/actions/profile`, COPY, Textarea, Input, Label, Button, ErrorBanner, and the `ProfileInput` type from `@/lib/db/schemas/profile`. Define a child `SubmitButton()` that reads `const { pending } = useFormStatus()` and renders the `h-12 w-full text-base font-medium` Button showing Loader2 + COPY.PROFILE_CTA_LOADING when pending, else COPY.PROFILE_CTA_SAVE (useFormStatus MUST be in a child of the form, not the form component itself). Export `function ProfileForm({ existing }: { existing: ProfileInput | null })`. Inside: `const [state, formAction] = useActionState(saveProfileAction, null)`. Render `<form action={formAction} className="space-y-6">` containing: heading COPY.PROFILE_EDIT_HEADING (`<h1 className="text-2xl font-semibold leading-[1.2] text-foreground">`), subtext COPY.PROFILE_EDIT_SUBTEXT (`text-sm text-muted-foreground`), `{state?.errors && <ErrorBanner message={COPY.AUTH_ERROR_SERVER} />}`, then all four fields with `name="goals"|"injuries"|"ftp"|"weight"` and `defaultValue={existing?.goals ?? ""}` (etc.; ftp/weight use `existing?.ftp ?? ""`). Goals + Injuries are Textarea (`text-base`, min-h as in UI-SPEC Screen 3); FTP `type="number" inputMode="numeric"`, Weight `type="number" inputMode="decimal"`, both `h-12 text-base` with unit hints COPY.FIELD_UNIT_FTP / COPY.FIELD_UNIT_WEIGHT. Close with `<SubmitButton />`. The save-success path is handled entirely by the Server Action (revalidate + redirect to /dashboard) — no client router push needed.

    Create src/app/(app)/profile/page.tsx as an async RSC. `getIronSession<SessionData>(await cookies(), sessionOptions)`; if `!session.id` redirect('/login'). `const profile = await findUserProfileByUserId(session.id)`. Render a layout matching dashboard (`bg-background`, single column, `max-w-lg`, `pt-16`): a "← Dashboard" back link (`text-sm font-medium text-foreground`, href="/dashboard") above a `Card`/`CardContent p-8` wrapping `<ProfileForm existing={...} />`. Pass to `existing` an object shaped like `ProfileInput` built from the row's `goals`/`injuries`/`ftp`/`weight` fields (or `null` when `profile` is null). The (app) layout already gates onboardingComplete, so any user reaching /profile has a profile row — but handle `profile === null` defensively by passing `null` (form renders empty defaults).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/profile/profile-form.tsx` starts with `"use client"`; uses `useActionState` from `react` and `useFormStatus` from `react-dom` in a separate `SubmitButton` child
    - Fields carry `name="goals"`, `name="injuries"`, `name="ftp"`, `name="weight"` and `defaultValue` reading from `existing` (grep each name returns ≥1)
    - `src/app/(app)/profile/page.tsx` calls `findUserProfileByUserId(session.id)` and renders `<ProfileForm existing={...} />`
    - All inputs/textareas carry `text-base`
    - `npx tsc --noEmit` passes; `npx vitest run` green; `npx next build` completes
  </acceptance_criteria>
  <done>ProfileForm pre-populates from existing profile and submits via the shared Server Action; /profile page fetches and renders it; build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dashboard FTP status + edit-profile link</name>
  <files>src/app/(app)/dashboard/page.tsx</files>
  <read_first>
    - src/app/(app)/dashboard/page.tsx (current stub — session read, max-w-sm container, logout form lines 16-50)
    - src/lib/db/queries.ts (findUserProfileByUserId)
    - src/lib/copy.ts (DASHBOARD_WELCOME, DASHBOARD_EMPTY_HEADING, DASHBOARD_EMPTY_BODY, DASHBOARD_FTP_ACTIVE, DASHBOARD_FTP_ABSENT, DASHBOARD_LINK_EDIT_PROFILE)
    - .planning/phases/02-profile-onboarding/02-UI-SPEC.md (Screen 2 Dashboard contract — FTP status line, empty-state copy, edit-profile link)
  </read_first>
  <action>
    Modify src/app/(app)/dashboard/page.tsx. Keep the existing session read and logout form. Add `import { findUserProfileByUserId } from "@/lib/db/queries"` and `import { COPY } from "@/lib/copy"`. After the session check, `const profile = await findUserProfileByUserId(session.id)`. Change the container from `max-w-sm` to `max-w-lg` per UI-SPEC Screen 2 (single column, keep centered layout). Render, in the `space-y-6` column: (1) the heading using COPY.DASHBOARD_EMPTY_HEADING ("You're all set") at `text-2xl font-semibold text-foreground` and body COPY.DASHBOARD_EMPTY_BODY at `text-sm text-muted-foreground` — this is the Phase 2 empty state (do NOT render placeholder session cards). (2) An FTP status line at `text-sm text-muted-foreground`: when `profile?.ftp` is a truthy number, render `COPY.DASHBOARD_FTP_ACTIVE.replace("{value}", String(profile.ftp))`; otherwise render COPY.DASHBOARD_FTP_ABSENT verbatim. (3) An "Edit profile" link (`<a href="/profile" className="text-sm font-medium text-foreground underline-offset-4 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring">`) using COPY.DASHBOARD_LINK_EDIT_PROFILE. Keep the existing logout form. You may keep displaying the signed-in email, but the FTP status line + edit link are the new required elements.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npx next build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard calls `findUserProfileByUserId(session.id)` (grep returns ≥1)
    - FTP-present branch substitutes the real watt value into DASHBOARD_FTP_ACTIVE (grep: `grep -c 'replace("{value}"' "src/app/(app)/dashboard/page.tsx"` returns ≥1) — the raw `{value}` token is NOT rendered to the user
    - FTP-absent branch renders DASHBOARD_FTP_ABSENT
    - An `href="/profile"` link is present using COPY.DASHBOARD_LINK_EDIT_PROFILE
    - `npx tsc --noEmit` passes; `npx vitest run` green; `npx next build` completes
  </acceptance_criteria>
  <done>Dashboard fetches profile, shows FTP-active or RPE-mode status with the real value substituted, and links to /profile; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser verification — full onboarding → edit → dashboard loop</name>
  <files>src/app/(app)/dashboard/page.tsx, src/app/(app)/profile/page.tsx, src/components/onboarding/onboarding-wizard.tsx</files>
  <action>
    No code changes. Apply the pending migration to the dev DB if not already applied: `DATABASE_URL_UNPOOLED=<direct-url> npx drizzle-kit migrate`. Start the dev server with `npm run dev`. Then present the manual verification steps below to the user and pause for their approval — this is a blocking human-verify gate. Do not mark the phase complete until the user confirms the full loop works.
  </action>
  <verify>
    <human-check>User confirms the onboarding → dashboard → edit-FTP → dashboard loop works per the steps in how-to-verify.</human-check>
  </verify>
  <what-built>
    The complete Phase 2 flow is wired: a new user (no completed profile) is forced through the 3-step wizard, lands on the dashboard with FTP status, and can edit their profile — including adding FTP later. All automated checks (tsc, vitest, next build) already pass from Tasks 1–2.
  </what-built>
  <how-to-verify>
    Apply the pending migration to the local/dev DB first if not already applied: `DATABASE_URL_UNPOOLED=<direct-url> npx drizzle-kit migrate`. Then run `npm run dev` and, using a fresh account that has NOT completed onboarding (or reset onboardingComplete=false on a test row):
    1. Visit http://localhost:3000/dashboard while logged in but unboarded → confirm you are redirected to /onboarding (no "too many redirects" error).
    2. Step 1: leave Goals blank, click Next → inline error "Describe your training goals to continue." appears; the textarea shows aria-invalid styling.
    3. Fill Goals, click Next → Step 2 (Injuries, optional). Leave blank, click Next → Step 3.
    4. Step 3: leave FTP and Weight blank, click "Get started" → button shows "Setting up your profile…" then you land on /dashboard showing "No FTP set · Using RPE mode" and an "Edit profile" link.
    5. Click "Edit profile" → /profile loads with Goals pre-populated; enter an FTP (e.g. 250), click "Save profile" → redirected to /dashboard now showing "FTP: 250W · Coggan zones active".
    6. Reload /dashboard → FTP status persists (profile row updated, not duplicated). Optionally verify the DB has exactly one user_profiles row for this user.
    7. Confirm the "Back" buttons in the wizard preserve typed values when navigating between steps.
  </how-to-verify>
  <resume-signal>Type "approved" once the full loop works (redirect gate, required-goals validation, optional skip, FTP-absent then FTP-added dashboard states, single non-duplicated row), or describe what failed.</resume-signal>
  <done>User has approved the full onboarding → edit → dashboard loop in the browser; PROF-02 upsert confirmed (single row), PROF-03 FTP/RPE dashboard states confirmed.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (Plan 03)

New files:
- `src/components/profile/profile-form.tsx` — exports `ProfileForm({ existing })` ('use client'); internal `SubmitButton`
- `src/app/(app)/profile/page.tsx` — default export `ProfilePage` (RSC)

Modified files / new behavior:
- `src/app/(app)/dashboard/page.tsx` — fetches profile, renders FTP-active vs RPE-mode status (value substituted into DASHBOARD_FTP_ACTIVE), adds "Edit profile" link, switches container to max-w-lg
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client form → Server Action | ProfileForm submits FormData to saveProfileAction (same boundary as Plan 02) |
| browser → /profile RSC | Authenticated profile read for pre-population |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering (IDOR) | /profile page + saveProfileAction | mitigate | Profile fetched via findUserProfileByUserId(session.id) — only the caller's own row; the form posts no userId; the Server Action re-reads userId from the session (defense in depth, not trusting the page). |
| T-02-07 | Information Disclosure | dashboard FTP status | accept | FTP/zone status is the user's own data shown only to the authenticated session owner; no cross-user exposure. Low risk, no PII beyond the user's own training metric. |
| T-02-04 | Elevation of Privilege | /profile + dashboard RSC | mitigate | Both pages call getIronSession and redirect('/login') on missing session.id; the (app) layout additionally gates onboardingComplete. |
| T-02-SC | Tampering | npm installs | mitigate | No new npm packages in this plan (all components reuse Plan 01/02 artifacts and existing shadcn components). |
</threat_model>

<verification>
- `npx next build` green for all three plans' routes
- `npx vitest run` full suite green (Plan 01 unit tests unaffected)
- Human-verify checkpoint confirms: redirect gate, required-goals validation, optional-field skip, FTP-absent → FTP-added dashboard transition, single non-duplicated profile row (PROF-02 upsert), Back-button value preservation
</verification>

<success_criteria>
- /profile pre-populates from the existing profile and saves via the shared Server Action (no duplicate row)
- FTP can be added after onboarding and the dashboard reflects it (PROF-02)
- Dashboard shows Coggan-zones-active when FTP present, RPE-mode when absent (PROF-03 display half)
- Full browser loop approved at the human-verify checkpoint
</success_criteria>

<output>
Create `.planning/phases/02-profile-onboarding/02-03-SUMMARY.md` when done
</output>
