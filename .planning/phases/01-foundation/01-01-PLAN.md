---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - drizzle.config.ts
  - vitest.config.ts
  - components.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/db/index.ts
  - src/lib/db/schema.ts
  - src/lib/session.ts
  - src/env.ts
  - tests/schema.test.ts
  - tests/auth.test.ts
  - tests/ratelimit.test.ts
  - tests/idor.test.ts
  - drizzle/
autonomous: false
requirements: [AUTH-01, AUTH-02]
user_setup:
  - service: neon
    why: "Primary Postgres data store; injects DATABASE_URL (pooled) and DATABASE_URL_UNPOOLED (direct)"
    env_vars:
      - name: DATABASE_URL
        source: "Vercel Marketplace -> Neon integration (pooled connection string)"
      - name: DATABASE_URL_UNPOOLED
        source: "Vercel Marketplace -> Neon integration (direct connection string, migrations only)"
    dashboard_config:
      - task: "Provision Neon Postgres via Vercel Marketplace and connect to the project"
        location: "Vercel Dashboard -> Storage -> Neon"
  - service: upstash
    why: "Redis-backed rate limiting for login endpoint (D-10)"
    env_vars:
      - name: UPSTASH_REDIS_REST_URL
        source: "Vercel Marketplace -> Upstash integration"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Vercel Marketplace -> Upstash integration"
    dashboard_config:
      - task: "Provision Upstash Redis via Vercel Marketplace and connect to the project"
        location: "Vercel Dashboard -> Storage -> Upstash"
  - service: app-secrets
    why: "iron-session cookie encryption requires a server-only secret"
    env_vars:
      - name: SESSION_SECRET
        source: "Generate ≥32 random bytes: `openssl rand -base64 32`; add to Vercel env (NOT NEXT_PUBLIC_)"
      - name: SIGNUP_ENABLED
        source: "Set to \"false\" for owner-locked launch; first-user bootstrap (D-11) bypasses it"

must_haves:
  truths:
    - "Next.js 16 app builds and runs locally with `npm run build`"
    - "All four tables (users, user_profiles, training_sessions, strava_connections) exist in Neon after migration"
    - "Every non-users table carries a user_id UUID FK referencing users.id with a dedicated index"
    - "iron-session config is exported from a single shared module with the { id, email } payload shape and D-06 cookie options"
    - "vitest runs and the four test stub files are discoverable (red, not missing)"
  artifacts:
    - path: "src/lib/db/schema.ts"
      provides: "All 4 Drizzle tables with user_id FK + index (D-01)"
      contains: "stravaConnections"
    - path: "src/lib/db/index.ts"
      provides: "Neon HTTP client bound to pooled DATABASE_URL (D-02)"
      contains: "neon-http"
    - path: "src/lib/session.ts"
      provides: "iron-session options + SessionData type (D-04, D-06)"
      contains: "sessionOptions"
    - path: "drizzle.config.ts"
      provides: "drizzle-kit config pointed at DATABASE_URL_UNPOOLED (D-02)"
      contains: "DATABASE_URL_UNPOOLED"
    - path: "vitest.config.ts"
      provides: "Test runner config with @/ path alias"
    - path: "tests/schema.test.ts"
      provides: "Asserts all non-users tables have user_id FK"
  key_links:
    - from: "src/lib/db/index.ts"
      to: "process.env.DATABASE_URL"
      via: "neon() HTTP driver"
      pattern: "neon\\(process\\.env\\.DATABASE_URL"
    - from: "drizzle.config.ts"
      to: "process.env.DATABASE_URL_UNPOOLED"
      via: "drizzle-kit dbCredentials"
      pattern: "DATABASE_URL_UNPOOLED"
---

<objective>
Scaffold the Next.js 16 app, define the complete Drizzle schema (all 4 app tables with user_id FK from day one per D-01), wire the Neon HTTP client and iron-session config as shared modules, apply the schema to Neon via a [BLOCKING] migration, and lay down the vitest test scaffolding (Wave 0 gap closure) that every subsequent plan verifies against.

Purpose: This is the foundation layer every other plan and every future phase depends on. The schema is locked here — changes after Phase 1 require migrations, so all 4 tables must be correct now (D-01). The session config and db client are single-source shared modules so route handlers, RSC layouts, and proxy.ts all import identical config.
Output: A buildable Next.js 16 project, a migrated Neon database with 4 tables, shared db/session modules, and 4 red test stubs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 project, install Phase 1 dependencies, init shadcn</name>
  <read_first>
    - CLAUDE.md §Technology Stack (authoritative versions — Next.js 16.2.9, Node 20.9+, all package versions)
    - CLAUDE.md §Critical Version Constraints Summary (proxy.ts, async cookies, Zod v4, DATABASE_URL)
    - .planning/phases/01-foundation/01-RESEARCH.md §Recommended Project Structure (src/ layout)
    - .planning/phases/01-foundation/01-UI-SPEC.md §Design System (shadcn dark + zinc, Tailwind v4)
  </read_first>
  <files>package.json, next.config.ts, tsconfig.json, src/app/layout.tsx, src/app/page.tsx, src/app/globals.css, components.json, src/env.ts</files>
  <action>
    Scaffold a Next.js 16.2.9 App Router project (TypeScript, Tailwind CSS v4, src/ directory, App Router, import alias `@/*`). Pin exact versions from CLAUDE.md: next 16.2.9, react 19.2, typescript 5.7+. Install Phase 1 runtime deps: @neondatabase/serverless 1.1.0, drizzle-orm 0.45.2, iron-session 8.0.4, bcryptjs 3.0.3, zod 4.4.3, @upstash/ratelimit 2.0.8, @upstash/redis 1.31.0 (pinned — compatible with @upstash/ratelimit 2.0.8). Install dev deps: drizzle-kit 0.31.10, vitest, @testing-library/react, @types/bcryptjs.
    Initialize shadcn with dark style + zinc base + CSS variables (`npx shadcn@latest init`), confirming generated CSS variables match the UI-SPEC color table (zinc-950 bg, zinc-900 card, zinc-50 text). globals.css uses `@import "tailwindcss"` (Tailwind v4 CSS-first config — no tailwind.config.js).
    Configure next.config.ts with the CSP + security headers from RESEARCH Pattern 8 (D-13): Content-Security-Policy, X-Content-Type-Options nosniff, X-Frame-Options DENY, applied via `async headers()` to `/(.*)`. Do NOT add any NEXT_PUBLIC_ prefix to secrets (D-13).
    Create src/env.ts that reads and type-narrows the authoritative env vars from CLAUDE.md (DATABASE_URL, DATABASE_URL_UNPOOLED, SESSION_SECRET, SIGNUP_ENABLED, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN) — server-only access, no NEXT_PUBLIC_.
    Replace the default home page with a minimal placeholder that links to /login.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - package.json lists next@16.2.9, drizzle-orm@0.45.2, iron-session@8.0.4, bcryptjs@3.0.3, zod@4.4.3, @upstash/ratelimit@2.0.8, @upstash/redis@1.31.0
    - components.json exists with style "default"/zinc base (shadcn initialized)
    - `grep -v '^#' src/app/globals.css | grep -c 'tailwindcss'` returns ≥1 (Tailwind v4 import present)
    - next.config.ts contains "Content-Security-Policy" and "X-Frame-Options"
    - No env var anywhere is prefixed NEXT_PUBLIC_ (`grep -rc 'NEXT_PUBLIC_' src next.config.ts` returns 0)
  </acceptance_criteria>
  <done>Project builds; shadcn + Tailwind v4 configured; CSP headers set; no secrets exposed to client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Drizzle schema (all 4 tables), Neon client, session module, drizzle config, and test scaffolding</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §Pattern 3 (schema — all 4 tables with user_id FK)
    - .planning/phases/01-foundation/01-RESEARCH.md §Pattern 1 (iron-session config)
    - .planning/phases/01-foundation/01-CONTEXT.md D-01, D-02, D-04, D-06
    - .planning/phases/01-foundation/01-VALIDATION.md §Wave 0 Requirements (4 test stubs)
  </read_first>
  <files>src/lib/db/schema.ts, src/lib/db/index.ts, src/lib/session.ts, drizzle.config.ts, vitest.config.ts, tests/schema.test.ts, tests/auth.test.ts, tests/ratelimit.test.ts, tests/idor.test.ts</files>
  <behavior>
    - tests/schema.test.ts: every non-users table (user_profiles, training_sessions, strava_connections) exposes a userId column referencing users.id, and the schema declares a matching index on user_id.
    - tests/schema.test.ts: users table has email (unique, notNull) and passwordHash (notNull) columns.
    - tests/auth.test.ts: stub describe blocks for signup (AUTH-01), login + cookie (AUTH-02), logout (AUTH-03), generic error, first-user bypass, registration-closed — marked failing/todo so they are red, not missing.
    - tests/ratelimit.test.ts: stub describe blocks for per-IP 10/15min and per-email 5/15min (AUTH-04) — red, not missing.
    - tests/idor.test.ts: stub describe block for "WHERE and() returns empty for cross-user query" (AUTH-05) — red, not missing.
  </behavior>
  <action>
    Create src/lib/db/schema.ts with all 4 tables exactly per D-01: `users` (id uuid pk defaultRandom, email text notNull unique, passwordHash text notNull, createdAt). `user_profiles`, `training_sessions`, `strava_connections` each with id uuid pk, `userId` uuid notNull references(() => users.id, { onDelete: "cascade" }), createdAt, and a named index on user_id. `strava_connections.userId` is additionally `.unique()` (one connection per user). Future-phase content columns are intentionally omitted (added later via migration) — leave a comment noting Phase 2/3/5 add columns here.
    Create src/lib/db/index.ts: export `db` from `drizzle(neon(process.env.DATABASE_URL!))` using `drizzle-orm/neon-http` and pooled DATABASE_URL (D-02). Add a comment that DATABASE_URL is pooled and migrations use UNPOOLED only.
    Create src/lib/session.ts per RESEARCH Pattern 1: export `SessionData` interface `{ id: string; email: string }` (D-04) and `sessionOptions` with cookieName "pace-session", password from SESSION_SECRET, cookieOptions httpOnly:true, secure:true, sameSite:"lax", maxAge 60*60*24*30 (D-06).
    Create drizzle.config.ts pointed at DATABASE_URL_UNPOOLED (D-02), schema "./src/lib/db/schema.ts", out "./drizzle", dialect "postgresql".
    Create vitest.config.ts with node environment and the `@/` path alias resolving to src/.
    Create the four test files per the behavior block. schema.test.ts imports the actual schema objects and asserts column/FK/index structure (real assertions, green-capable once schema exists). The other three are red stubs (`it.todo` or `expect(false).toBe(true)` with a comment referencing the implementing plan) so the Nyquist sampler sees them as red, not missing.
  </action>
  <verify>
    <automated>npx vitest run tests/schema.test.ts --reporter=verbose</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'references' src/lib/db/schema.ts` returns ≥3 (three FK references to users.id)
    - schema.ts contains "strava_connections", "training_sessions", "user_profiles", and "user_id" index declarations
    - src/lib/session.ts contains `sameSite: "lax"`, `httpOnly: true`, `maxAge` (D-06) and `SessionData` with id + email (D-04)
    - drizzle.config.ts contains "DATABASE_URL_UNPOOLED"
    - src/lib/db/index.ts uses "drizzle-orm/neon-http" and `process.env.DATABASE_URL` (pooled, not UNPOOLED)
    - `npx vitest run tests/schema.test.ts` passes (schema assertions green)
    - tests/auth.test.ts, tests/ratelimit.test.ts, tests/idor.test.ts exist and run red (not "no test found")
  </acceptance_criteria>
  <done>Schema with 4 tables + FKs + indexes; db/session/drizzle config modules; vitest config; 4 test files present (schema green, others red).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [BLOCKING] Apply Drizzle migration to Neon</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 4 (migrate against UNPOOLED, not pooled)
    - .planning/phases/01-foundation/01-CONTEXT.md D-02
  </read_first>
  <what-built>
    Schema files are complete. This task generates and applies the migration to the Neon database. It is [BLOCKING] because no subsequent verification can pass without the tables existing in Neon. Requires DATABASE_URL_UNPOOLED to be set (Neon provisioned via Vercel Marketplace — see user_setup). If env vars are not yet present, this is the gate where the user provisions Neon + Upstash and sets SESSION_SECRET / SIGNUP_ENABLED.

    Migration commands (run with DATABASE_URL_UNPOOLED set explicitly; non-TTY safe):
    - `DATABASE_URL_UNPOOLED=... npx drizzle-kit generate`
    - `DATABASE_URL_UNPOOLED=... npx drizzle-kit migrate`
  </what-built>
  <how-to-verify>
    1. Confirm DATABASE_URL_UNPOOLED, DATABASE_URL, SESSION_SECRET, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN are set in the environment / Vercel project.
    2. Run `npx drizzle-kit generate` — confirm a migration SQL file appears under drizzle/.
    3. Run `npx drizzle-kit migrate` against DATABASE_URL_UNPOOLED — confirm it reports applied migrations with no error.
    4. Verify in Neon (SQL console or `psql`): `users`, `user_profiles`, `training_sessions`, `strava_connections` tables exist; each non-users table has a `user_id` column and an index on it.
  </how-to-verify>
  <acceptance_criteria>
    - A migration SQL file exists under drizzle/ containing `CREATE TABLE "users"` and the three other tables
    - `drizzle-kit migrate` reports success
    - All 4 tables queryable in Neon; user_id indexes present on the 3 non-users tables
  </acceptance_criteria>
  <resume-signal>Type "migrated" once all 4 tables exist in Neon, or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → app | Browser requests cross into Next.js server; no trusted input |
| app → Neon | Drizzle queries cross into the database over HTTP |
| build → client bundle | Env vars must not cross into client-side JS (D-13) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Elevation of Privilege | schema.ts table definitions | mitigate | All non-users tables carry user_id FK + index from day one (D-01); future per-user scoping has a column to filter on. Verified by tests/schema.test.ts. |
| T-1-IDOR-FK | Elevation of Privilege | users.id FK references | mitigate | onDelete cascade ensures orphaned cross-user rows cannot persist after user deletion. |
| T-1-SECRET | Information Disclosure | next.config.ts / env.ts | mitigate | No NEXT_PUBLIC_ prefix on any secret (D-13); CSP + X-Frame-Options DENY headers set. Verified by grep gate. |
| T-1-MIGRATE | Tampering | drizzle-kit migration | mitigate | Migrations run against DATABASE_URL_UNPOOLED only (D-02, Pitfall 4); pooled URL never used for DDL. |
| T-1-SC | Tampering | npm installs | mitigate | All packages [CITED: CLAUDE.md] in RESEARCH Package Legitimacy Audit (verdict OK); no [ASSUMED]/[SUS]/[SLOP] packages. |
</threat_model>

<verification>
- `npm run build` succeeds
- `npx vitest run tests/schema.test.ts` green; other 3 test files red (present)
- 4 tables migrated to Neon with user_id FK + index on the 3 non-users tables
- No NEXT_PUBLIC_ on any secret; CSP headers present
</verification>

<success_criteria>
- Next.js 16 app builds and runs
- Drizzle schema defines all 4 tables with user_id FK + index (D-01)
- Migration applied to Neon (all 4 tables exist)
- Shared db client (pooled), session config (D-04/D-06), drizzle config (UNPOOLED) exist
- vitest scaffolding in place (Wave 0 gap closed): schema test green, auth/ratelimit/idor stubs red
</success_criteria>

<artifacts>
## Artifacts this phase produces (Plan 01)

- `package.json`, `next.config.ts`, `tsconfig.json`, `components.json` — Next.js 16 + Tailwind v4 + shadcn project
- `src/app/globals.css`, `src/app/layout.tsx`, `src/app/page.tsx` — app shell + dark zinc theme
- `src/lib/db/schema.ts` — all 4 Drizzle tables with user_id FK + index (D-01)
- `src/lib/db/index.ts` — Neon HTTP client (pooled DATABASE_URL, D-02)
- `src/lib/session.ts` — iron-session config + SessionData type (D-04, D-06)
- `src/env.ts` — typed server-only env access (D-13)
- `drizzle.config.ts` + `drizzle/` migrations — schema migration (UNPOOLED, D-02)
- `vitest.config.ts` + `tests/{schema,auth,ratelimit,idor}.test.ts` — test scaffolding (Wave 0)
</artifacts>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
