---
phase: 03-ai-session-generation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/lib/ai/prompt.ts
  - src/lib/ai/compute-watts.ts
  - src/lib/actions/session.ts
  - tests/generate-session.test.ts
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]
must_haves:
  truths:
    - "@anthropic-ai/sdk is installed and importable"
    - "The system prompt is at least 1,024 tokens so cache_control activates"
    - "User free-text (goals, injuries) is wrapped in XML delimiters in the user prompt, never concatenated into the system prompt"
    - "generateSessionAction reads userId from iron-session, never from client input"
    - "generateSessionAction runs auth → profile → rate limit → AI → Zod → safety → DB write → return in that order"
    - "With FTP set, targetWatts is computed server-side as round(powerFraction * ftp)"
    - "Without FTP, powerFraction is computed server-side as targetWatts / 150"
    - "On Zod or safety failure the action returns the generic 'Couldn't generate a valid session' message; on API error it returns 'Generation failed'"
  artifacts:
    - path: "src/lib/ai/prompt.ts"
      provides: "SYSTEM_PROMPT (≥1024 tokens), buildUserPrompt, READINESS_LABELS"
      exports: ["SYSTEM_PROMPT", "buildUserPrompt"]
    - path: "src/lib/ai/compute-watts.ts"
      provides: "computeWattTargets — fills targetWatts/powerFraction per FTP presence (D-02)"
      exports: ["computeWattTargets"]
    - path: "src/lib/actions/session.ts"
      provides: "generateSessionAction Server Action (D-08, D-09)"
      exports: ["generateSessionAction"]
  key_links:
    - from: "src/lib/actions/session.ts"
      to: "@anthropic-ai/sdk"
      via: "anthropic.messages.create with system array + cache_control"
      pattern: "messages\\.create"
    - from: "src/lib/actions/session.ts"
      to: "src/lib/safety-gate.ts"
      via: "validateSessionSafety after Zod safeParse"
      pattern: "validateSessionSafety"
    - from: "src/lib/actions/session.ts"
      to: "src/lib/ratelimit.ts"
      via: "generationLimiter.limit(userId) before AI call"
      pattern: "generationLimiter\\.limit"
---

<objective>
Install the Anthropic SDK and build the server-side generation pipeline: the cache-controlled system prompt + XML-delimited user prompt (D-05/D-06/D-07), the server-side watt/power computation (D-02), and the `generateSessionAction` Server Action that orchestrates the full D-09 execution order (auth → profile → rate limit → Anthropic call → Zod validate → safety gate → DB write → return).

Purpose: This is the trust-boundary core of the phase. The action is the single point where untrusted AI output is validated through both gates from Plan 01 before persistence, where the daily rate limit is enforced before spend, and where user free-text is isolated as data via XML delimiters to prevent prompt injection.
Output: `@anthropic-ai/sdk` installed, `SYSTEM_PROMPT` + `buildUserPrompt`, `computeWattTargets`, and `generateSessionAction` returning `{ data?: ...; error?: string }`, with a test asserting the gate ordering and error contract via a mocked SDK.
</objective>

<artifacts_produced>
## Artifacts this phase produces (new symbols created by Plan 02)

- `src/lib/ai/prompt.ts` — `SYSTEM_PROMPT` (string constant ≥1024 tokens), `buildUserPrompt(profile, readinessScore)`, `READINESS_LABELS` (0–3 label map)
- `src/lib/ai/compute-watts.ts` — `computeWattTargets(blocks, ftp): blocks` — fills `targetWatts` from `powerFraction * ftp` (FTP set) or `powerFraction` from `targetWatts / 150` (FTP absent), per D-02; exports the `NO_FTP_REFERENCE_WATTS = 150` constant (internal — never displayed)
- `src/lib/actions/session.ts` — `generateSessionAction(readinessScore: number): Promise<{ data?; error? }>` Server Action
- `package.json` / `package-lock.json` — `@anthropic-ai/sdk` dependency added
- `tests/generate-session.test.ts` — new test file (mocked Anthropic SDK)
</artifacts_produced>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-ai-session-generation/03-CONTEXT.md
@.planning/phases/03-ai-session-generation/03-RESEARCH.md
@.planning/phases/03-ai-session-generation/03-PATTERNS.md
@.planning/phases/03-ai-session-generation/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @anthropic-ai/sdk + build cache-controlled prompt module (D-05/D-06/D-07) + watt computation (D-02)</name>
  <files>package.json, package-lock.json, src/lib/ai/prompt.ts, src/lib/ai/compute-watts.ts</files>
  <read_first>
    - src/lib/db/schemas/session.ts (GeneratedSession block shape — input/output type for computeWattTargets)
    - src/env.ts (ANTHROPIC_API_KEY export — server-only import source)
    - .planning/phases/03-ai-session-generation/03-CONTEXT.md (D-02 compute branching, D-05/D-06 system prompt, D-07 user prompt + readiness labels)
    - .planning/phases/03-ai-session-generation/03-RESEARCH.md (Pattern 1 system array + cache_control; Pitfall 1 token threshold; Pitfall 4 150W reference)
  </read_first>
  <action>
    Install the SDK: `npm install @anthropic-ai/sdk@0.104.1` (pinned version per CLAUDE.md §AI Integration; RESEARCH.md Package Legitimacy Audit verdict: official Anthropic SDK, Approved). This adds it to `dependencies` — it is currently NOT in package.json despite CLAUDE.md listing it. No `checkpoint:human-verify` required: the audit classifies it SUS-by-automation only (recency of latest release), not a legitimacy concern, and it is the official anthropics/anthropic-sdk-typescript package.

    Create `src/lib/ai/prompt.ts`:
    - Export `SYSTEM_PROMPT` as a server-side string constant. Content per D-06: cycling-coach role; explicit JSON-only output contract embedding the D-03 schema field-by-field; the instruction "Respond with ONLY a valid JSON object. No markdown. No explanation. No code fences." (Pitfall 3); no-FTP conservative watt guidance ("Beginner: 100–150W. Returning from injury: 80–130W."); interval-structure guidance (warmup → work intervals → cooldown; rest blocks between high-intensity work). The prompt MUST be ≥1,024 tokens for `cache_control: {type:"ephemeral"}` to activate (Pitfall 1) — pad with coaching philosophy, per-block-type explanations (warmup/work/rest/cooldown), and schema documentation until comfortably above threshold (target 1,200+ tokens). Exact prose is Claude's discretion; the ≥1,024-token length is mandatory.
    - Export `READINESS_LABELS` mapping 0→"Flat — very fatigued", 1→"OK — some fatigue", 2→"Good — feeling capable", 3→"Fresh — ready to push" (D-07).
    - Export `buildUserPrompt(profile, readinessScore)` returning the D-07 template with user free-text wrapped in XML delimiters: a `<user_profile>` block (Goals, FTP as "{ftp}W" or "Not set (RPE mode)", Weight as "{weight}kg" or "Not set"), an `<injury_notes>` block ({injuries or "None"}), then "Readiness today: {readinessScore}/3 ({label})", "Date: {ISO date}", and the generate instruction. User-controlled fields (goals, injuries) go ONLY inside the XML delimiters — never into SYSTEM_PROMPT (prompt-injection mitigation, T-03-05; PROJECT.md §AI prompt safety).

    Create `src/lib/ai/compute-watts.ts` exporting `computeWattTargets(blocks, ftp: number | null)` per D-02: for each block, if `ftp` is present compute `targetWatts = Math.round(block.powerFraction * ftp)` (Claude supplies powerFraction); if `ftp` is null compute `powerFraction = block.targetWatts / NO_FTP_REFERENCE_WATTS` (Claude supplies targetWatts). Define and export `const NO_FTP_REFERENCE_WATTS = 150` with a comment: internal Phase 4 .zwo-export reference only — NEVER displayed to the user as their FTP (Pitfall 4). Return the blocks with both fields populated. Type the parameter and return against the `GeneratedSession["blocks"]` element type from `@/lib/db/schemas/session`.
  </action>
  <verify>
    <automated>node -e "require.resolve('@anthropic-ai/sdk')" &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').dependencies['@anthropic-ai/sdk'])"` prints a version (not undefined)
    - `src/lib/ai/prompt.ts` contains `export const SYSTEM_PROMPT`, `export function buildUserPrompt`, and the literal `<user_profile>` and `<injury_notes>`
    - `src/lib/ai/prompt.ts` contains the literal "No code fences" (Pitfall 3 instruction present)
    - `src/lib/ai/compute-watts.ts` contains `export function computeWattTargets` and `NO_FTP_REFERENCE_WATTS = 150`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>SDK installed; prompt module with ≥1024-token cache-eligible system prompt and XML-delimited user prompt; watt-computation utility implementing the D-02 dual path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: generateSessionAction Server Action — full D-09 pipeline (with mocked-SDK test)</name>
  <files>src/lib/actions/session.ts, tests/generate-session.test.ts</files>
  <read_first>
    - src/lib/actions/profile.ts (exact Server Action header, 'use server', iron-session auth gate, Zod safeParse+.issues, DB write pattern to mirror)
    - src/lib/db/queries.ts (findUserProfileByUserId — read profile; findLatestSessionByUserId from Plan 01)
    - src/lib/db/schemas/session.ts (GeneratedSessionSchema.safeParse, GeneratedSession type)
    - src/lib/safety-gate.ts (validateSessionSafety — runs AFTER Zod, D-09 order)
    - src/lib/ratelimit.ts (generationLimiter.limit(userId) — block before AI call)
    - src/lib/ai/prompt.ts and src/lib/ai/compute-watts.ts (SYSTEM_PROMPT, buildUserPrompt, computeWattTargets from Task 1)
    - src/env.ts (ANTHROPIC_API_KEY — import from @/env, never process.env in action)
    - src/lib/db/schema.ts (trainingSessions insert columns)
    - tests/profile.test.ts (existing action test style — describe/it, vi.mock approach)
    - .planning/phases/03-ai-session-generation/03-RESEARCH.md (Code Examples: full execution-order skeleton; Pitfalls 2,3,5,6,7)
  </read_first>
  <behavior>
    - test: when generationLimiter.limit resolves `{ success: false }`, the action returns `{ error: "Daily limit reached. Try again tomorrow." }` and the Anthropic SDK is NOT called (no spend)
    - test: when the mocked SDK returns valid JSON matching GeneratedSessionSchema, the action calls validateSessionSafety, inserts into trainingSessions, and returns `{ data }`
    - test: when the mocked SDK returns JSON with powerFraction 2.5 (fails Zod), the action returns `{ error: "Couldn't generate a valid session. Please try again." }` and does NOT insert
    - test: when the mocked SDK returns Zod-valid JSON that fails the safety gate (e.g. powerFraction 1.6), the action returns the same generic "Couldn't generate" error and does NOT insert
    - test: when the mocked SDK throws, the action returns `{ error: "Generation failed. Please try again in a moment." }`
    - test: when there is no session.id (unauthenticated), the action does not call the SDK (auth gate fires first)
  </behavior>
  <action>
    Create `src/lib/actions/session.ts` with `'use server'` and the security-contract file header (userId from iron-session only; ANTHROPIC_API_KEY from @/env; AI output validated through both gates before any DB write). Mirror `src/lib/actions/profile.ts` structure.

    Implement `generateSessionAction(readinessScore: number): Promise<{ data?: typeof trainingSessions.$inferSelect; error?: string }>` in strict D-09 order:
    1. Auth: `const session = await getIronSession<SessionData>(await cookies(), sessionOptions)`; if `!session.id` return `{ error: "Not authenticated" }` (the page is already gated; do not redirect from inside this imperative action). `userId = session.id`.
    2. Profile: `const profile = await findUserProfileByUserId(userId)`.
    3. Rate limit: `const limit = await generationLimiter.limit(userId)`; if `!limit.success` return `{ error: "Daily limit reached. Try again tomorrow." }` — BEFORE any Anthropic call (Pitfall 6: key is userId).
    4. AI call: construct `new Anthropic({ apiKey: ANTHROPIC_API_KEY })`; `anthropic.messages.create({ model: "claude-sonnet-4-6", max_tokens: 2048, system: [{ type: "text", text: SYSTEM_PROMPT, cache_control: { type: "ephemeral" } }], messages: [{ role: "user", content: buildUserPrompt(profile, readinessScore) }] })`. `system` MUST be the array form for cache_control (RESEARCH Pattern 1 — string form silently drops caching). Wrap in try/catch; on throw return `{ error: "Generation failed. Please try again in a moment." }`. Extract `rawText = msg.content[0].type === "text" ? msg.content[0].text : ""` (Pitfall 2). Optionally log `msg.usage.cache_creation_input_tokens` server-side to confirm cache activation.
    5. Zod validate: strip markdown fences defensively (`rawText.replace(/^```json?\n?/, "").replace(/\n?```$/, "")`, Pitfall 3), `JSON.parse` in try/catch (parse failure → generic "Couldn't generate a valid session. Please try again."), then `GeneratedSessionSchema.safeParse`. On `!success`: `console.error("Zod issues:", result.error.issues)` (server-log only, .issues not .errors) and return the generic error. Never surface issue messages to the client.
    6. Safety gate: `validateSessionSafety(result.data)`; on `!safe`: `console.error("Safety gate failed:", reason)` and return the generic error.
    7. DB write: `const blocksWithWatts = computeWattTargets(result.data.blocks, profile?.ftp ?? null)`; `db.insert(trainingSessions).values({ userId, title, notes, readinessScore, blocks: blocksWithWatts as unknown as JsonValue, totalDurationSec, rawJson: rawText }).returning()` (cast blocks per Pitfall 7). Do NOT call `revalidatePath` — it triggers the Next.js 15+ useTransition/isPending hang (RESEARCH Pattern 6); the UI renders from the returned data.
    8. Return `{ data: inserted }`.

    Create `tests/generate-session.test.ts` mocking `@anthropic-ai/sdk`, `next/headers` (cookies), `iron-session` (getIronSession returning a session with id), and `@/lib/ratelimit` (generationLimiter.limit). Mock `@/lib/db/index` so `.insert().values().returning()` is observable without a live DB. Assert all six behavior cases above. Follow the `vi.mock` style in `tests/profile.test.ts`. Place in `tests/` (project convention).
  </action>
  <verify>
    <automated>npx vitest run tests/generate-session.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/actions/session.ts` starts with `'use server'` and contains `export async function generateSessionAction`
    - File contains `generationLimiter.limit`, `GeneratedSessionSchema.safeParse`, `validateSessionSafety`, and `computeWattTargets` called in that source order
    - File contains `cache_control` and `system: [` (array form) and `claude-sonnet-4-6`
    - `grep -c 'revalidatePath' src/lib/actions/session.ts` returns 0
    - File imports `ANTHROPIC_API_KEY` from `@/env` (not `process.env` directly)
    - `npx vitest run tests/generate-session.test.ts` exits 0 with all six behavior cases green, including the assertion that the SDK is not called when the rate limit blocks
  </acceptance_criteria>
  <done>generateSessionAction implements the full D-09 pipeline with both validation gates, rate limit before spend, server-side watt computation, and generic error contract; mocked-SDK test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user free-text → AI prompt | goals/injuries are untrusted free text; must be data (XML-delimited), not instructions |
| AI output → DB write | Claude JSON is untrusted; validated by Zod + safety gate before insert |
| client → Server Action | readinessScore is the only client input; userId derives from cookie, never client |
| app → Anthropic API | ANTHROPIC_API_KEY must stay server-side (never in client bundle) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Tampering | Prompt injection via goals/injuries (buildUserPrompt) | mitigate | User free-text wrapped in `<user_profile>`/`<injury_notes>` XML delimiters; SYSTEM_PROMPT is a static server constant and never receives user input (PROJECT.md §AI prompt safety) |
| T-03-01 | Tampering | AI output → DB (GeneratedSessionSchema.safeParse) | mitigate | Action calls safeParse before insert; Zod-fail returns generic error, no insert |
| T-03-02 | Tampering | Unsafe session (validateSessionSafety) | mitigate | Action runs the safety gate after Zod, before insert (D-09 order, Pitfall 5) |
| T-03-03 | Denial of Service | Runaway AI spend (generationLimiter) | mitigate | `generationLimiter.limit(userId)` checked BEFORE the Anthropic call; blocked requests never reach the SDK |
| T-03-06 | Information Disclosure | ANTHROPIC_API_KEY in client bundle | mitigate | Imported from `@/env` in a `'use server'` module; no `NEXT_PUBLIC_` prefix (Phase 1 D-13) |
| T-03-07 | Information Disclosure | raw_json / technical error leakage to client | mitigate | `rawJson` stored server-only and never returned to the UI; Zod issues / safety reason logged server-side only; client gets generic messages |
| T-03-04 | Elevation of Privilege | Cross-user profile read | mitigate | `findUserProfileByUserId(userId)` uses cookie userId only; no client-supplied id |
| T-03-SC | Tampering | npm install `@anthropic-ai/sdk@0.104.1` | accept | RESEARCH.md Package Legitimacy Audit: official anthropics package, 23.5M downloads/wk, SUS-by-automation (release recency) only — Approved, no [SLOP], no human checkpoint required |
</threat_model>

<verification>
- `npx vitest run` — full suite green (Plan 01 tests + new generate-session test, no regressions)
- `npx tsc --noEmit` — no type errors
- `@anthropic-ai/sdk` resolvable; no API key in any client-imported module
</verification>

<success_criteria>
- @anthropic-ai/sdk installed and importable (GEN-01)
- System prompt ≥1024 tokens, cache_control array form (GEN-01)
- User free-text XML-delimited; userId from cookie only (GEN-01, security)
- D-09 pipeline order enforced; both gates run before DB write (GEN-02)
- Rate limit blocks before AI call (GEN-03)
- Server-side watt/power computation per D-02
- generate-session test green across all six behavior cases
</success_criteria>

<output>
Create `.planning/phases/03-ai-session-generation/03-02-SUMMARY.md` when done
</output>
